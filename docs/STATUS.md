# FITOM リファクタリング 完了ステータス

## 完成したファイル一覧

### プラグイン SDK (`plugin_sdk/`)
| ファイル | 状態 | 内容 |
|---|---|---|
| `IHWPlugin.h` | ✅ | HW I/F DLL C API |
| `IMidiPlugin.h` | ✅ | MIDI バックエンド DLL C API |

### コアライブラリ (`core/`)

#### インフラ層
| ファイル | 状態 | 内容 |
|---|---|---|
| `include/fitom/fitom_core.h` | ✅ | stdafx.h 代替・共通インクルード |
| `include/fitom/IPort.h` / `IPort.cpp` | ✅ | ハードウェア I/O 抽象 |
| `include/fitom/PluginLoader.h` | ✅ | DLL 動的ロード RAII |
| `include/fitom/HWPort.h` / `.cpp` | ✅ | HWPlugin → IPort アダプター (実機/エミュレータ共通、HWPluginRegistryで複数管理) |
| `include/fitom/MidiManager.h` / `.cpp` | ✅ | MIDI バックエンド DLL 管理 |
| `include/fitom/Log.h` / `Log.cpp` | ✅ | Boost.Log ラッパー |
| `include/fitom/VolumeUtils.h` / `.cpp` | ✅ | CalcLinearLevel / Linear2dB / ROM テーブル |
| `include/fitom/FnumUtils.h` | ✅ | F-number テーブルキャッシュ |
| `include/fitom/AudioEngine.h` / `.cpp` | 🗑️ 廃止 | fitom_fmhwif DLL に移管 |

#### ボイスデータ
| ファイル | 状態 | 内容 |
|---|---|---|
| `include/fitom/VoiceData.h` | ✅ | HwPatch / SwPatch / FmHwOp / FmSwOp |
| `include/fitom/VoiceProcessor.h` / `.cpp` | ✅ | ベロシティ感度・ソフト LFO 処理 |
| `include/fitom/PatchData.h` | ✅ | Patch / ToneLayer / HwBank / SwBank |
| `include/fitom/PatchManager.h` / `.cpp` | ✅ | バンク管理・JSON I/O |

#### 音源デバイス
| ファイル | 状態 | 内容 |
|---|---|---|
| `include/fitom/ISoundDevice.h` / `SoundDevImpl.cpp` | ✅ | CSoundDevice 共通実装 |
| `OPN_new.cpp` | ✅ | COPN (YM2203, FXモード対応) |
| `OPM_new.cpp` | ✅ | OPM / OPP / OPZ (REV/EGS対応) |
| `OPN2_new.cpp` | ✅ | COPNA/COPN2 (CSpanDevice、6ch) / COPNB (YM2610無印、ch0/ch3無効化した実効4ch) / COPNARhythm |
| `OPL_new.cpp` | ✅ | OPL/OPL2/COPL3(4OPモード)/COPL3_2(2OP、CSpanDevice)/COPLRhythm(内蔵リズム5パート) |
| `OPLL_new.cpp` | ✅ | OPLL/OPLL2/OPLLP/OPLLX/VRC7/COPLLRhythm |
| `PSG_new.cpp` | ✅ | SSG/DCSG/SCC (CPSGBaseはSW-EG/SW-LFO共通化のみ) |
| `MultiDev_new.cpp` / `include/fitom/MultiDevice.h` | ✅ | CMultiDevice/CSpanDevice/CUnison (ヘッダー化済み) |
| `ADPCM_new.cpp` | ✅ | CYmDelta(Y8950/OPNA/OPNB)/CAdPcm2610A/CAdPcmZ280 |
| `include/fitom/DeviceFactory.h` / `.cpp` | ✅ | IPort → ISoundDevice ファクトリ |

#### MIDI 処理
| ファイル | 状態 | 内容 |
|---|---|---|
| `include/fitom/MidiCh.h` / `MidiCh.cpp` | ✅ | CInstCh / CRhythmCh (マルチレイヤー) |

#### 設定・コア
| ファイル | 状態 | 内容 |
|---|---|---|
| `include/fitom/Config.h` / `Config.cpp` | ✅ | FITOMConfig (ISoundDevice 対応版) |
| `include/fitom/CFITOM.h` / `CFITOM.cpp` | ✅ | コアシングルトン / MidiProcessor |

### バックエンド DLL

| ファイル | 状態 | 内容 |
|---|---|---|
| `backends/midi_rtmidi/` | ✅ | RtMidi (Windows/Linux/macOS共通、2026年7月に旧midi_wms/midi_winmm/midi_alsaの3実装から統合) |
| `backends/midi_pipe/` | ✅ | 内部用MIDIパイプ (`fitom_midi_pipe`、名前付きパイプ/UNIXソケット、パッチエディタ連携用、既定OFF)。最大16本まで同時接続対応(2026年7月〜、接続ごとに専用スレッド)。接続確立直後、チャンネル割り当てをプライベートSysEx(sub-cmd 0x03)で通知し、クライアント側は自分でチャンネルを選ばない設計に変更(複数パッチエディタインスタンス同時起動時の衝突回避)。詳細は`plugin-midi-pipe.md`参照 |
| `backends/hw_if/CMakeLists.txt` | ✅ | FitomIFTest submodule 統合 |

### GUI

| ファイル | 状態 | 内容 |
|---|---|---|
| `gui/bridge/FITOMBridge.h` | ✅ | UIフレームワーク非依存のコアブリッジAPI |
| `gui/bridge/FITOMBridge.cpp` | ✅ | ブリッジ実装 |
| `apps/fitom_gui/` | 🚧 | Dear ImGui + GLFW + OpenGL3 導入済み。ルート画面のMIDIモニターバンド(CH毎のBank/Program/Volume/Note/Device/Fnumber表示、MPU切替、128ノートキーボードビュー+発光エフェクト)を実装済み。Bank/Program表示のダブルクリックで外部パッチエディタ(別リポジトリ`FITOM_patch_editor`、実行ファイルは`fitom_gui`と同じディレクトリに配置想定)をキオスクモード(`<profile.json> <kind> <bank-file> <prog>`、`kind`="device"/"layered"/"drum"、D-039/D-040)で子プロセス起動する機能を実装済み(パッチエディタ未検出・起動失敗時はImGuiモーダルでエラー表示。実機での起動確認は未実施、下記STATUS.md注記参照)。CH番号のシングルクリックでCH設定ダイアログ(`ChSettingsDialog`)を開き、Volume(CC#7)/Expression(CC#11)/リズム⇔インストゥルメント切替(CC#0特殊値)/Poly⇔Mono切替(CC#126/127)/パッチ選択を変更できる機能を実装済み。パッチ選択はCC#0→CC#32→Prog.chgの階層ブラウジングを行う`PatchPickerDialog`(直接デバイス選択モードも対応)、リズムチャンネルはドラムキットのフラット一覧を別途実装済み。適用ロジックはGUI側から`IMidiCh`のsetterを個別に叩かず、`FITOMBridge::sendControlChange`/`sendProgramChange`経由でコアの既存MIDI処理経路(`MidiProcessor::processControl`/`IMidiCh::progChange`)を再利用する方式(2026年7月)。パッチピッカーのProg.chg階層は、行をマウスボタンダウンした瞬間にCC#0/CC#32/Prog.chg/Note On(C4、`FITOMBridge::sendNoteOn`/`sendNoteOff`新設)を送り、ボタンアップでNote Offを送る「押している間だけ鳴る」試聴動作を実装済み(`ImGui::IsItemActivated`/`IsItemDeactivated`でボタンダウン/アップを直接検出。同じ行への連打も含め、押すたびに必ず送り直す)。確定は「選択」ボタンのみで行う(以前はダブルクリックでも確定していたが、連打時に試聴のNote Onと確定側のNote Offが競合し音が鳴らなくなる不具合があり、2026年7月に確定操作を「選択」ボタンへ一本化して解消)。確定時は試聴中の値のままNote Offのみ送り(通常は既にボタンアップで止まっている)、ピッカー/CH設定いずれのキャンセルでも試聴前(open()時点)のCC#0/CC#32/Prog.chgへ復元するメッセージを送り直す。CH設定ダイアログのVolume/Panpot/Expressionスライダーも操作するたびにCC#7/#10/#11を即時送信してプレビューでき、キャンセル時は開いた時点の値へ復元する(2026年7月)。MIDIモニターのバンク名・パッチ名解決は、直接デバイス選択モード(HwBankRegistry参照)にも対応済み(2026年7月修正。以前は通常モード[PatchBank]とリズムチャンネルのみ対応で、直接モードのチャンネルは常に数値フォールバック表示になっていた)。MIDIモニターのMIDIポート名部分のクリックでMIDIポート設定ダイアログ(`MidiPortSettingsDialog`)を開き、MPU(`CFITOM::getMpuCount()`、現状4)分のMIDI入力ポート割り当てをそれぞれドロップダウン(システムが現在列挙するポート名一覧、`FITOMBridge::getAvailableMidiInputPorts()`)から変更できる機能を実装済み(2026年7月新設)。バリデーションは重複設定(複数ポートへの同一MIDI IN割り当て)チェックのみで、違反時はエラーメッセージボックスを表示してOKを継続させない。OKで閉じると`FITOMBridge::setMidiInputPorts()`が既存の全MIDI入力ポートを閉じてから選択内容で開き直し(即時反映)、`FITOMConfig`側の設定(現在のプロファイル状態)を更新したうえで、`FITOMBridge::saveCurrentProfile()`経由で現在ロード中のプロファイルファイルへ書き戻す。この変更に伴い、コア側(`CFITOM::init()`)もMIDI入力ポート数に関わらず常にMAX_MPUS(4)分のMidiProcessor/チャンネルを生成するよう修正した(以前は設定済みポート数分しか生成せず、未設定のMPUには実行中に新規ポートを割り当てられなかった)。MIDIモニター左上の歯車アイコンボタン(`gearIconButton()`、日本語フォントのグリフ収録範囲に依存しないようImDrawListへの直接描画で実装)からシステム設定ダイアログ(`SystemSettingsDialog`)を開き、マスターボリューム・マスターピッチ(430〜450Hz)を変更できる機能を実装済み(2026年7月新設。ChSettingsDialogのVolume/Panpot/Expressionスライダーと同じくドラッグ中は即時プレビュー、キャンセルで開いた時点の値へ復元)。こちらもOKで`FITOMBridge::saveCurrentProfile()`によりプロファイルファイルへ書き戻す。プロファイルスキーマに`master_volume`/`master_pitch`フィールドを新設し(`config_schema/profile.schema.json`)、`FITOMConfig::buildFromProfile()`で読み込み・`FITOMConfig::saveProfile()`で書き戻す(ロード時のJSON全体を`profileJson_`に保持しておき、GUIから変更されるフィールドのみ上書きして書き出すことで、devices/hw_plugins/banks等の他のフィールドはそのまま維持される)。あわせて、`FITOMBridge::setMasterPitch()`が従来`FnumRegistry`を直接叩いておりConfig側の値が更新されず発音中チャンネルへのF-number即時反映も行われていなかったバグを、`CFITOM::setMasterPitch()`経由に修正した。デバイス一覧・パッチ一覧(フラット表示)等、他画面への導線は未着手(該当描画関数は`[[maybe_unused]]`で温存) |

### 設定スキーマ・ドキュメント

| ファイル | 状態 |
|---|---|
| `fitom.conf.schema.json` | ✅ |
| `profile.schema.json` | ✅ |
| `hwbank.schema.json` | ✅ |
| `swbank.schema.json` | ✅ |
| `patchbank.schema.json` | ✅ |
| `docs/DESIGN.md` | ✅ |
| `docs/chip-driver-migration.md` | ✅ |
| `docs/chip-driver-architecture.md` | ✅ |
| `docs/patch-structure-design.md` | ✅ |
| `docs/voice-data-design.md` | ✅ |
| `docs/config-design.md` | ✅ |
| `docs/plugin-hwif.md` | ✅ |
| `docs/plugin-midi.md` | ✅ |
| `docs/plugin-midi-pipe.md` | ✅ |
| `docs/midi-implementation-status.md` | ✅ |
| `docs/terminology.md` | ✅ |
| `docs/voice-parameter-reference.md` | ✅ |
| `docs/manuals/midi-message-reference.md` | ✅ |
| `docs/manuals/midi-implementation-chart.md` | ✅ |
| `docs/manuals/hwpatch-reference.md` | ✅ |
| `docs/manuals/swpatch-reference.md` | ✅ |
| `docs/manuals/layered-patch-reference.md` | ✅ |

---

## 音源機能の実装状況（追加セッション分）

以下の機能は初期リファクタリング完了後、追加セッションで実装・修正した。

| 機能 | 状態 | 関連ドキュメント |
|---|---|---|
| ベロシティ感度 (VTL + VAR〜VRR、全FMチップ + PSG) | ✅ | `voice-data-design.md` |
| ソフトウェアLFO 全面再設計 (LfoControl) | ✅ | `voice-data-design.md` |
| CC#1 Modulation → LFR=0音色専用のCC駆動LFO | ✅ | `midi-implementation-status.md` |
| マスターピッチ可変 (430-450Hz) + OPM算出バグ修正 | ✅ | — |
| ダイナミックボイスアサイン (findBestCh 1パス化) | ✅ | — |
| Sustain (CC#64) チップ依存実装 + MIDI配線バグ修正 | ✅ | `midi-implementation-status.md` |
| Sostenuto (CC#66) | ✅ | `midi-implementation-status.md` |
| Portamento/Legato モノフォニック専用化 + バグ修正 | ✅ | `midi-implementation-status.md` |
| Portamento Rate テーブル刷新 (GM2グラフ準拠) + fine_ セント単位遷移対応 | ✅ | `midi-implementation-status.md` |
| CC#120 forceDamp (全チップ、ALGキャリア判定込み) | ✅ | `midi-implementation-status.md` |
| VoicePatchType システム (音色パッチ互換性分類) | ✅ | `patch-structure-design.md` |
| バンクセレクトLSB直接指定モード | ✅ | `patch-structure-design.md` |
| PSGソフトウェアエンベロープ (SoftEnvelope, FM実機準拠ADSR) | ✅ | `voice-data-design.md` |
| AY-3-8910 HW EGレジスタ仕様修正 (ext.HWEP) | ✅ | `voice-data-design.md` |
| OPLLX / VRC7 (6ch専用) チップドライバ | ✅ | — |
| リズムモード汎用フィールド (`rhythm_mode`) | ✅ | `config-design.md` |
| RtAudio削除 (fitom_fmhwif DLLへ移管) | ✅ | `plugin-hwif.md` |
| HWデバイス レイテンシ同期 (GetLatencySamples/SetDelaySamples) | ✅ | `plugin-hwif.md` |
| Sub-device自動生成 (OPNA→FM+SSG+ADPCM-B+Rhythm 等) | ✅ | `chip-driver-architecture.md` |
| 同種デバイス自動束ね (CSpanDevice、VoicePatchType基準) | ✅ | `chip-driver-architecture.md` |
| リニアステレオ化 (CLinearPanDevice、`stereo_pair`の明示指定でL/R2台を等パワーパンニングのステレオ1台に束ねる。deviceType基準・composite展開チップ対応)。左右の分離方法は2方式: `true`=プラグイン側の出力ルーティング(`pan:1/2`、モノラルチップでも可) / `"L"`・`"R"`=チップ自身のL/R出力ビット(OPM/OPZ/OPL3/OPL4[FM部]/OPN2/OPNA/OPNB/OPNBB系、プラグイン側は`pan:0`のままでよくOPL4のAWM部が自前のパンポットをフル活用できる) | ✅ | `chip-driver-architecture.md`, `config-design.md` |
| OPL3 4OPモード (COPL3) + 疑似デチューン(DT2転用) | ✅ | `chip-driver-architecture.md` |
| OPN FXモード (3rd channel special mode、疑似デチューン/非整数倍率/固定周波数) | ✅ | `chip-driver-architecture.md`, `voice-data-design.md` |
| COPNARhythm / COPLLRhythm (内蔵リズム音源、独立レジスタ体系) | ✅ | `chip-driver-architecture.md` |
| CPSGBase 責務整理 (SW-EG/SW-LFO共通化のみに純化、SSG固有コードをCSSGへ移動) | ✅ | `chip-driver-architecture.md` |
| リリース中再トリガー対策 (wasReleasing、OPM/OPN/OPL/OPL3) | ✅ | `chip-driver-architecture.md` |
| ADPCM RegMap 全面修正 (Y8950/OPNA/OPNB個別マップ、memory/panmaskフィールド追加) | ✅ | `chip-driver-architecture.md` |
| OPLL Fnumberビットシフト修正 | ✅ | `chip-driver-architecture.md` |
| OPLL EGT/RR静的変換 (`SR>0`ならEGT=0+RRレジスタ=SR。キーオン/キーオフ時の動的書き換えは廃止しOPL系とは別方式。実機OPLLのEG挙動がOPL系と異なることを2026年8月に実機確認済みの**確定仕様**) | ✅ | `chip-driver-architecture.md`, `voice-parameter-reference.md`, `manuals/hwpatch-reference.md` |
| HWPlugin_Shutdown (未エクスポート時は何もしないオプショナルAPI、二重実行防止) | ✅ | `plugin-hwif.md` |
| GUI MIDIモニターバンド (CH毎表示 + 128ノートキーボードビュー + 発光エフェクト) | ✅ | — |
| GUI CH設定ダイアログ + パッチピッカーダイアログ (Volume/Expression/リズム⇔インストゥルメント切替/Poly⇔Mono切替/CC#0→CC#32→Prog.chg階層ブラウジング + Prog.chg選択時の試聴(Note On C4)・キャンセル時の復元。GUIからは`FITOMBridge`のMIDI送信メソッド経由でコアの既存MIDI処理経路を再利用) | ✅ | — |
| 内部用MIDIパイプ (`fitom_midi_pipe`、パッチエディタ試聴連携) | ✅ | `plugin-midi-pipe.md` |
| 内部用MIDIパイプの多接続化(最大16本+接続直後のチャンネル自動割り当てSysEx) | ✅ | `plugin-midi-pipe.md` |
| MIDIバックエンドDLLをRtMidi単一実装へ統合 (旧midi_wms/midi_winmm/midi_alsaの3実装廃止、SysEx未対応だった既存欠陥を解消、macOS対応を新規追加) | ✅ | `plugin-midi.md` |
| OPNB(YM2610無印)の誤分類修正 (VOICE_PATCH_OPNからOPN2側へ、COPNB新設でSSG/ADPCM-Aサブデバイス自動生成・実効4ch化に対応。ステージング環境からの指摘で発覚) | ✅ | `chip-driver-architecture.md` |
| `CSoundDevice::chState_`等、チャンネル数を固定長配列で持っていた箇所のvector化 (OPL4 AWM=24chがMAX_CHS=16固定配列を超えて範囲外アクセスするクラッシュを修正。`chState_`本体に加え`CPSGBase::lfoTL_`/`envelopes_`、`CLinearPanDevice::masterVolume_`/`masterPan_`も同種のためvector化。ステージング環境での「無言で強制終了」報告から発覚) | ✅ | `chip-driver-architecture.md` |
| GUI MIDIポート設定ダイアログ (`MidiPortSettingsDialog`、MIDIモニターのポート名クリックで開く。MPU 4面分のMIDI入力ポート割り当てをドロップダウンで変更、重複設定のみバリデーション、OKで即時反映+プロファイル書き戻し。`CFITOM::init()`をMIDI入力ポート数に関わらず常に4MPU分生成するよう修正し、実行中の未使用MPUへのポート割り当てに対応) | ✅ | `config-design.md` |
| GUI システム設定ダイアログ (`SystemSettingsDialog`、MIDIモニター左上の歯車アイコンボタンで開く。マスターボリューム/マスターピッチ(430〜450Hz)をスライダーで即時プレビュー、OKでプロファイル書き戻し。プロファイルスキーマに`master_volume`/`master_pitch`を新設し、`FITOMConfig::saveProfile()`(新設、ロード時JSONへの差分上書き書き戻し)経由で永続化。`FITOMBridge::setMasterPitch()`がConfig/発音中チャンネルへの反映を素通りしていたバグも合わせて修正) | ✅ | `config-design.md` |
| レジスタダンプモニターのコア層基盤(2026年7月新設)。`HWPort::write()`/`writeBurst()`が実際にHWPlugin_Write/WriteBlockへ渡した値をそのまま`shadowRegs_`(mutex保護、addr=0x0000-0xFFFF全域)にミラーする「最後に書き込んだ値」のキャッシュを新設(`HWPort::getShadowReg()`/`getShadowRegRange()`)。実チップにはレジスタ読み出しAPIが無いため、あくまでFITOM_Xが最後に書き込んだ値を返す点に注意。`CFITOM`に物理チップ単位の列挙(`PhysicalChipInfo`、`getPhysicalChipCount()`/`getPhysicalChipInfo()`/`getPhysicalChipRegisterDump()`)を新設し、サブデバイス自動生成(OPNA→FM+SSG+ADPCM-B等、同一`HWPort`を共有)や同種デバイス自動束ね(spanGroups)・リニアステレオ化(stereoPairPort)で生成される複数の論理`ISoundDevice`を、物理チップ単位(`buildPhysicalChipList()`が`initDevices()`末尾でHWPortポインタの同一性から判定)にまとめて1エントリとして扱う。2ポートチップ(OPN2/OPL3等)は`getPhysicalChipRegisterDump()`がport1を0x000-0x0FF、port2を0x100-0x1FFにpackして返す。`HWPort`は`HWPlugin_Open()`に渡したparams_json(type/serial/port/slot、またはFMHWIFのengine/chip等)も保持し、`getPhysicalChipName()`でhwif接続情報由来の物理チップ名(例:"SPFM_TOWER COM3 slot0"、"YMEngine/OPNA")を組み立てて返す(`PhysicalChipInfo::physicalName`)。**Fix(2026年7月)**: 高位ポート(0x100以降)を使うチップが実際に接続されている物理HWポートは1つだけ(SPFM 2スロット等の`extra_slot`を使わない一般的な構成)の場合に、高位アドレス側が表示されない不具合を修正。OPNA/OPN2系(`OPN2Port2`/`OffsetPort`経由で内部的に同一`HWPort`へ+0x100して書く)やOPL3(addr>=0x100を直接同一`HWPort`へ書く)は、`config_`側に別々の`port`/`port2`が存在しなくても0x100-0x1FFへの書き込みは同じ`HWPort`のシャドウレジスタに記録されているため、`port2`の有無だけでダンプ範囲を判定していたのが原因。`PhysicalChipInfo::dumpSize`を新設し、`port2`が無い場合も`CFITOM::getDeviceRegSize(deviceType)`(kDevMapの既知のレジスタ空間サイズ)が0x100を超えていれば、その分だけ同一`HWPort`から一括で読み出すよう修正した | ✅ | — |
| レジスタダンプモニターのBridge API・GUI(2026年7月新設、同月中に表示方式を変更)。`FITOMBridge::getHwChips()`/`getHwChipRegisterDump()`でコア層の物理チップ列挙・レジスタダンプを公開(`FITOMChipInfo::physicalName`はhwif接続情報由来の物理チップ名。当初`descriptor`としてFITOM_X内部のチップドライバ分類=論理チップ名を返していたが、GUIでの目視確認で違和感が指摘され物理チップ名に差し替えた)。`apps/fitom_gui`に`RegisterDumpWindow`を新設し、MIDIモニター左上の歯車アイコン隣の「REG」ボタンでMIDIモニター本体とルート画面上でオルタネート表示(排他的に切り替え)する(当初は独立した別ウィンドウとして重ねて表示していたが、実際にGUIを使ったユーザーからの指摘によりMIDIモニターとの排他表示に変更。ボタンは表示中「MIDI」に切り替わり元に戻せる)。物理チップごとに16進数生値のグリッド(`ImGui::Table`、1ポートは16行、2ポートは32行)を表示し、直前フレームとの差分検出で値が変化したセルは`ImGui::TableSetBgColor`でセル背景を発光させ、`renderKeyboardView()`の発音グロー(main.cpp、ベロシティ連動の発光エフェクト)と同じ「発光開始時刻からの経過時間でフェード」方式(0.6秒)で徐々に消える。表示専用(値の編集不可)。**Fix(2026年7月)**: 各チップのテーブルに`ImGuiTableFlags_ScrollY`+行数からの高さ見積もりで固定高さを与えていたが、見積もりがセルパディング分だけ実際の内容より小さく、バンド(チップ)ごとに常にスクロールバーが出てしまっていた不具合をユーザー指摘で修正。テーブルの高さ指定・ScrollYを廃止し、行数分へ自然にフィットさせる(自動サイズ)方式に変更。ウィンドウ全体の内容が窓の高さを超えた場合はMIDIモニターバンドと同じくルートウィンドウ側のスクロールに任せる | ✅ | — |
| チャンネルレベルメーターのコア層基盤(2026年7月新設。Bridge API・GUIは未着手、下記「既知の未対応」参照)。レジスタダンプモニターを左右2ペイン化し、左に「チップごとの物理チャンネル1本ずつのバー(ベロシティ/TL連動の疑似メーター。FITOM_Xは音声合成を行わないため実音量信号は存在せず、`ChState::isActive()`+`velocity`による疑似表示である点に注意)」、右に既存のレジスタダンプを表示する計画の土台。`PhysicalChipSubDevice`(`device`/`deviceType`/`chCount`)を新設し、`PhysicalChipInfo::subDevices`として物理チップ1個を構成する論理`ISoundDevice`群(例: OPNBならFM+SSG+ADPCM-A+ADPCM-B)の内訳を`buildPhysicalChipList()`のメインループ(`config_->getDeviceCount()`)で記録する(stereoPairPort/spanGroups経由で束ねられる物理チップは、`devices_[i]`が`CLinearPanDevice`/`CSpanDevice`にラップされ`getChCount()`が複数物理チップ分を合算してしまうため対象外、既知の制限)。`CFITOM::getPhysicalChipChannelStates()`で全チャンネル分の現在の発音状態(`PhysicalChipChannelState{sounding,velocity}`)を`subDevices`の並び順で取得できる。チャンネル名は`CFITOM::getSubDeviceChannelPrefix(deviceType)`(新設の`kChannelPrefixMap`、OPN系→"FM"、OPL3→"4OP"、OPL3_2→"2OP"、ADPCM-A→"PA"、ADPCM-B→"PB"、SSG→"SSG"、OPL4AWM→"AWM"等)を「接頭辞+(ch+1)」で組み立てる想定(呼び出し側で組み立てる、コア側は接頭辞のみ提供)。表示順(サブデバイスの並び)は現状Config側の生成順のままで、ユーザー例示の並び(FM→PA→PB→SSG)とは一致しない場合があり、GUI実装時に表示用の並べ替えが必要になる見込み。**物理/論理表示切替(2026年7月追加)**: チャンネルレベルメーターのみ対象(レジスタダンプは実バイト値表示のため常に物理ポート単位のまま)。`CFITOM::getLogicalDeviceChannelStates(deviceIndex)`を新設し、`getPhysicalChipChannelStates()`(同一物理ポートを共有する全サブデバイスをまとめた状態)とは別に、`devices[]`の1エントリ(=1論理デバイス)単独のチャンネル状態を返せるようにした。論理チップ側の一覧・ラベルは既存の`getDevices()`(`FITOMDeviceInfo`)とチャンネル名は共通の`getSubDeviceChannelPrefix()`をそのまま流用でき、新規データモデルの追加は不要だった。**Bridge API・GUI(2026年7月追加)**: `FITOMBridge`に`FITOMLevelChannel{name,sounding,velocity}`/`FITOMLevelBand{label,channels}`と`getPhysicalLevelBands()`/`getLogicalLevelBands()`を新設(チャンネル名はBridge側で「接頭辞+(ch+1)」を組み立て済みの状態で返す)。`apps/fitom_gui`に`LevelMeterPanel`を新設し、`RegisterDumpWindow`(旧・単独ウィンドウ)と共に左右2ペイン化(`ImGui::BeginChild`2枠、左42%/右残り、各々独立スクロール)。左ペインの`LevelMeterPanel`は「物理」「論理」ボタンで`getPhysicalLevelBands()`/`getLogicalLevelBands()`を切り替え、バンドごとに`ImGui::SeparatorText`+チャンネル数分の縦バー(`ImDrawList`直接描画、発音中はベロシティに応じた緑塗り、バー下部にチャンネル名)を12本折り返しで表示する | ✅ | — |
| チャンネルレベルメーターのカラーLED風表示化・ノートオン取りこぼし修正(2026年7月、ソリッドバーからの見た目改善要望+実機確認で「ノートオンをかなり取りこぼしているように見える」と報告され発覚)。**表示**: `LevelMeterPanel`のバー描画をソリッド塗りつぶしから14分割セグメント(下から緑8/黄3/赤3ゾーン、消灯セグメントも各ゾーン色の暗いバリエーションで塗る)のLED風表示に変更。バーのレベルはノートオン/オフに連動する疑似減衰エンベロープ(GUI側`ChannelEnvelope`、チャンネルごとに`unordered_map`で保持)で駆動し、ノートオンでベロシティ由来のピークへ即座に立ち上がり1.5秒で全減衰、ノートオフ時点でその時のレベルから250msで全減衰する。ノートオン時はピークホールド(ピーク位置のセグメントを白側にブレンドして際立たせる)を重ね、1.5秒で全減衰・ノートオフで即時消滅させる(`main.cpp`の鍵盤ビュー発光エフェクトと同じ「経過時間からのフェード計算」方式)。**ノートオン取りこぼし修正**: 当初はノートオンのエッジ検出を`FITOMLevelChannel::sounding`のfalse→true遷移または`velocity`変化で行っていたが、`sounding`/`velocity`はブリッジが返す現在値のスナップショットに過ぎないため、ボイススチールによる同一物理チャンネル上の連続ノートオン(同一ベロシティでの再トリガー、`sounding`はtrueのまま・`velocity`も不変)を検出できず取りこぼしていた。真因の解決には現在値スナップショットの比較では原理的に不十分なため、`ChState`にノートオンのたびに単調増加する`noteOnSeq`(`uint32_t`)を新設し、`CSoundDevice::noteOn()`でインクリメント、`PhysicalChipChannelState`→`FITOMLevelChannel`まで伝播させ、GUI側のノートオン検出を`noteOnSeq`の変化(フレーム間差分)で行うよう変更した(ノートオフの検出は従来通り`sounding`のtrue→false遷移のまま)。この修正に合わせ、従来`enabled`の伝播が抜けていた`CFITOM::getLogicalDeviceChannelStates()`(物理側の`getPhysicalChipChannelStates()`には既にあった)も揃えて修正した | ✅ | — |
| チャンネルレベルメーターのステレオL/R分割表示(2026年8月新設)。ステレオ出力を持つチャンネルは、バー1本を左右half-widthの2本に分割し、定位係数(`gainL`/`gainR`、大きい側が1.0になるよう正規化)を掛けた高さでそれぞれ描く(バーの高さ自体はベロシティ由来のエンベロープで、発音イベントはL/R共通の1本を使う)。分割するかどうかはビューの単位に従う: **物理チップ単位**ではリニアステレオ化ペアをL側・R側の独立したモノラルバンドのまま並べ(物理チップ1台=モノラルであり、右ペインのレジスタダンプ一覧とも一対一で対応する)、**論理チップ単位**では束ねた結果の1デバイスとして左右分割表示にする(定位は`ISoundDevice::getStereoGains()`をCLinearPanDeviceが実装して返す)。チップ自身がL/R出力を持つ場合(`FITOMConfig::getChipPanType()`が`Mono`以外)は両ビューとも分割表示で、定位は`ChState::panpot`から算出する(`ThreeWay`は各ドライバの`updatePanpot()`と同じ閾値、`Continuous`[OPL4 AWM/YMZ280B/SAA1099]は等パワーパンニングの式)。併せて、リニアステレオ化したチップがレベルメーターに一切表示されなかった既知の制限を解消した(`createLeveledDevice()`が`CLinearPanDevice`へラップする前のL/R生デバイスを呼び出し元へ返せるようにし、`pendingSubDevices_`へそれぞれの物理ポートで登録する)。物理/論理の振り分けはユーザーの実機スクリーンショットでの確認を経て確定 | ✅ | `chip-driver-architecture.md` |
| PCMバンク複数併用対応・パッチピッカー用named patch自動合成(2026年7月新設、ステージング環境で「ADPCM-Aのパッチピッカーが常に空、試聴すると発音解決失敗」と報告され発覚)。`banks.pcm_banks[]`に任意の`group`(ADPCMB/ADPCMA/PCMD8)を追加。従来`CFITOM::initDevices()`が全PCMデバイスへ`setPcmRegistry(reg, 0)`とバンク番号0を決め打ちしていたため、2つ目以降の`pcm_banks`エントリ(コーデックの異なるADPCM-A用バンク等)が常に無視される不具合があった。`group`指定時はそのVoicePatchTypeに一致するPCMデバイスへ対応バンク番号を自動的に割り当てる(`PcmBankRegistry::findBankNoForVoicePatchType()`、未指定時はbank0固定の旧動作のまま後方互換)。また`group`指定時、`PatchManager::loadPcmBankJson()`が`entries[]`(adpcm_packer出力由来、各エントリの`root_note`も反映)の各サンプルからnamed patchを自動合成するため、`*.samplezonebank.json`を別途手書きしなくても、そのままパッチピッカー(CC#0=ADPCM-B/ADPCM-A/PCMD8)から個々のサンプルを選択・試聴できる。**ADPCM-B/ADPCM-A/PCMD8はHwPatchではなくAWMと同じ`SampleZonePatch`スキーマ(`isSampleBasedVoicePatchType()`)を使う設計のため、自動合成先は`HwBankRegistry`ではなく`SampleZoneBankRegistry`である点に注意**(実装時に一度`HwBankRegistry`側へ誤って合成し、パッチピッカーの表示は直るが実際のNoteOn解決が失敗する`SampleZonePatch not found`という不具合を作り込みかけた)。この修正に合わせ、GUI側(`FITOMBridge::getHwBankList()`/`getHwBankPatches()`、MIDIモニターのバンク/パッチ名解決)も`isSampleBasedVoicePatchType()`で分岐し`sampleRegistry()`を参照するよう修正(以前はサンプルベース音源系のバンク一覧が常に空になっていた、AWMも同様の恩恵を受ける) | ✅ | `patch-structure-design.md`, `hwpatch-reference.md` |
| ADPCM-A/ADPCM-B/PCMD8の波形メモリ配置の責務誤り修正、および`CMultiDevice`(CSpanDevice/CUnison)のPCM/波形レジストリ未伝播バグ修正(2026年7月、「ADPCM-A/Bともに正常に発音していない。キーオンフラグは操作されているが波形アドレスが正しく設定されていない」との報告で発覚)。調査は2段階だった:(1) `CAdPcmBase::loadVoice()`(`CYmDelta`/`CAdPcm2610A`/`CAdPcmZ280`共通のパターン)が波形バイナリ(adpcm_packer出力の.bin)をチップのPCM RAM相当レジスタへFITOM_X側から逐次書き込む実装になっていたこと自体が設計上の誤りだったため、`loadVoice()`を`registerVoice()`(voices_[WS番号]テーブルへオフセット/サイズを登録するだけ、レジスタ書き込みなし)に置き換え、実チップへのPCMバイナリ転送処理を全廃した(波形データの配置はhwif側の責務、FITOM_X本体はStart/Endアドレスレジスタを設定するだけでよいという設計に統一)。(2) しかしこれでも症状が解消せず、ユーザーが`CAdPcm2610A::updateVoice()`にデバッグログを仕込んで調査した結果、`voices_[]`テーブル自体が一度も登録されていないことが判明。**真因は`ISoundDevice::setPcmRegistry()`/`initPcmData()`がデフォルトno-op実装であるのに対し、複数チップを束ねる`CMultiDevice`(`CSpanDevice`/`CUnison`)がこれらをオーバーライドしておらず、束ねられた実チップ(サブチップ)へ一切伝播していなかったこと**。ADPCM-Aのように2枚の物理OPNBチップに跨って「同種デバイス自動束ね」(spanGroups、VoicePatchType基準)される構成では、`CFITOM::initDevices()`が`dev->setPcmRegistry(...)`/`dev->initPcmData()`を呼ぶ`dev`は束ね役の`CSpanDevice`自身であり、それが基底のno-opで止まってしまうため、実際に発音する`CAdPcm2610A`インスタンス側の`voices_`が空のまま(Start/Endアドレス=0)だった。`CMultiDevice`に`setWaveRegistry()`/`setPcmRegistry()`/`initPcmData()`を追加し、`chips_`の全サブチップへブロードキャストするよう修正(SCCの`setWaveRegistry()`も同じ未伝播バグを抱えていたため合わせて修正)。3チップドライバ共通のロジックのため、ADPCM-B/ADPCM-A/PCMD8すべてに同じ修正を適用。`core/include/fitom/PcmBankData.h`冒頭のワークフロー説明コメントも合わせて修正 | ✅ | — |
| ADPCM-A(YM2610/2610B)・ADPCM-B(YM2608=OPNA)のポートアドレス誤り修正(2026年7月、上記の一連のADPCM修正を経てもなお発音せず、ユーザーが「OPNA/OPNB/OPNBBのADPCMA/ADPCMBポートアドレスが間違っている、本来port2側[アドレス0x100以降]に書くべき値がport1側になっている」と指摘し発覚)。`Config::resolveCompositeSpec()`が生成する`DEVICE_ADPCMB_OPNA`/`DEVICE_ADPCMA`のサブデバイスは`usesExtraPort=false`のままだったため、`DeviceFactory::create()`に渡る`port`は常にSSG/FM共通と同じport1(低位、アドレス0x000-0x0FF)だった。だが実チップ上、ADPCM-A(YM2610/2610B、レジスタ範囲がSSGの0x00-0x0Dや`kOPNB_DeltaT`の0x10-0x1cと衝突する)・ADPCM-B(YM2608=OPNAのみ、一部のBRDY割り込み等の制御ビットはport1側の`COPNA::init()`が個別に設定するが、Start/End/Delta-N/Volume等の主要レジスタ群はport2側)は、実際にはport2(高位、アドレス0x100以降)に配置されるレジスタ体系である(ADPCM-B[YM2610/2610B側、`kOPNB_DeltaT`]は逆にport1のままで正しく対象外)。`Config::resolveCompositeSpec()`の該当`SubDeviceSpec`を`usesExtraPort=true`に変更し、`CFITOM`に`resolveAdpcmHighPort()`(`offsetPorts_`で寿命管理する`OffsetPort(port,0x100)`を、プロファイルで明示的な2スロットHW構成[`extra_port`]が無い場合に自前生成する)を新設して`initDevices()`のメインループ・spanGroupサブチップ生成ループ双方に適用した。`OPN2_new.cpp`の`COPNB`クラスコメント(「YM2610無印はADPCM-B用メモリ空間を持たないため生成しない」)も、上の`resolveCompositeSpec()`修正時に追従できていなかった古い記述だったため合わせて訂正。(2026年7月追記: 同種の問題が`DEVICE_OPL4AWM`[OPL4のAWM/PCM部]でも発覚。FM部[`DEVICE_OPL3`/`DEVICE_OPL3_2`]が使うport1/port2の2バンク[アドレス0x000-0x1FF]とは独立した3つ目のレジスタバンク[アドレス0x200以降]に配置されるべきところ、`usesExtraPort=false`のままport1と衝突していた。既存の2ポート[`port`/`port2`]モデルでは3バンク目を表現できないため、`resolveAdpcmHighPort()`を`resolveHighBankPort()`に汎化し、deviceType判定で`OffsetPort(port,0x200)`をOPL4AWM用に自前生成するよう拡張して解消。さらにユーザー指摘で、GUIのレジスタダンプモニター[`RegisterDumpWindow`]側も追従が必要と発覚: `CFITOM::buildPhysicalChipList()`の物理チップ表示サイズ[`dumpSize`]決定ロジックが、同一物理ポートを共有する複数サブデバイスのうち**最初に登録されたものだけ**を見ており[OPL4なら先に登録される`DEVICE_OPL3`のレジスタ空間0x200のみ]、後から登録される`DEVICE_OPL4AWM`の高位バンク[0x200-0x2FF]がダンプ表示範囲から漏れていた。オフセット量を`resolveHighBankPort()`と共有する`getHighBankOffset()`を新設し、`buildPhysicalChipList()`側も登録順に関わらず全サブデバイスの必要サイズ[オフセット+`getDeviceRegSize()`]の最大値を採用するよう修正[`kDevMap`に`DEVICE_OPL4AWM`のレジスタ空間サイズ0x100も追加]。`RegisterDumpWindow.cpp`側はダンプの実サイズから表示範囲を動的に組み立てる設計だったため変更不要だった。さらに2026年8月、ユーザーから「AWMは音は出るが意図した波形ではない」と報告があり、`COPL4AWM::updateVoice()`の波形番号レジスタ書き込み順バグが発覚。YMEngineが使うエミュレーションコア`ymfm`(`extern/ymfm/src/ymfm_pcm.cpp`の`pcm_engine::write()`)は、reg 0x08-0x1F(波形番号下位8bit)への書き込みで即座に`load_wavetable()`をトリガーし、その時点のreg 0x20+ch bit0(波形番号上位1bit)と組み合わせて9bit波形番号を確定させる。`updateVoice()`はreg 0x08(下位8bit)をreg 0x20(bit8)より先に書いていたため、新しい波形番号のbit8が反映される前(=1つ前のノートのbit8のまま)でロードがトリガーされ、意図した波形と256ずれた別の波形が鳴っていた。ユーザー指定のALSAドライバ`sound/drivers/opl4/opl4_synth.c`の`snd_opl4_note_on()`でも、`OPL4_REG_F_NUMBER`(reg 0x20+ch、波形番号bit8)を`OPL4_REG_TONE_NUMBER`(reg 0x08+ch)より先に書いており、後者の呼び出し箇所に明示的に「triggers header loading」とコメントされている(実チップ・エミュレータ双方でこの順序が必須であることの裏付け)。`updateVoice()`内の書き込み順をreg 0x20→reg 0x08に入れ替えて解消し、波形番号がbit8境界(256)をまたいで切り替わる場合の回帰テストを追加した。**(2026年8月さらに追記)** 上記修正後もユーザーから「まだ意図した波形ではないような鳴り方」と報告があり、ALSAドライバとFnumber/音量計算を突き合わせて2件の根本原因を特定・修正した。**(A) Fnumber/Octave計算式そのものがAWM用ではなかった**: `COPL4AWM::getFnumber()`は共通の`getFnumberFromHz()`(OPN/OPM系FM合成のFnum位相累算器用の式、`fnumMaster_`/`fnumDivide_`という無関係な定数を使い、Octaveを0〜7[常に非負]にクランプする)を誤って流用していた。実際のAWMエンジン(`extern/ymfm/src/ymfm_pcm.cpp`)は`step=((0x400|fnum)<<(octave+7))>>2`という全く別の式で、Octave(reg 0x38 bit7-4)は符号付き4bit(-8〜+7、`int8_t(ch_octave<<4)>>4`で符号拡張)。ALSAの`snd_opl4_update_pitch()`(`sound/drivers/opl4/opl4_synth.c`)も`octave=pitch/0x600-8`と明示的に符号付き計算しており、両者と一致する専用の計算式に書き換えた(基準点note=60・オフセット0で`pitch=60*128=7680→octave=-3,fnum=0`となることをALSAと同一の独立した手計算値で検証する回帰テストを追加)。**(B) 波形ごとの校正データ(pitch_offset/key_scaling/tone_attenuate/volume_factor)が欠落**: ROM波形は実測でないと絶対ピッチ・音量が分からない(ヘッダにその情報が無い)ため、(A)を直しただけでは波形によって数オクターブ単位でピッチがずれたままだった。ALSAの`sound/drivers/opl4/yrw801.c`(YRW801実機向けに実測・埋め込まれた表、`opl4_sound`構造体の`tone`/`pitch_offset`/`key_scaling`/`tone_attenuate`/`volume_factor`等)をPythonスクリプトで機械的にパースし、`SampleZone`に追加した同名4フィールド(`pitchOffset`/`keyScaling`/`toneAttenuate`/`volumeFactor`、`config_schema/samplezonebank.schema.json`にもスキーマ追加)へ、既存の`config/profiles/opl4awm_yrw801_gm/drum.samplezonebank.json`の各ゾーンを`wave_index`(+key範囲)完全一致でマージした(GM 553ゾーン・ドラム57ゾーンとも欠落・曖昧一致ゼロで全件マッチ。既存のwave_index/key範囲データ自体が2026年7月時点で既にこのALSA表から機械抽出されたものだったため、想定通り高いマッチ率になった)。`COPL4AWM::getFnumber()`/`updateVolExp()`はこれらの値を解決したゾーンから読み、ALSAの`snd_opl4_update_pitch()`/`snd_opl4_update_volume()`と同じ規約で適用する) | ✅ | `chip-driver-architecture.md` |
| ADPCM-B(Delta-T)再生レート・アドレス境界・センターパン無音・マスタークロックの4件を修正(2026年7月、上記のポートアドレス修正後もADPCM-Bが正常に再生されず、実機ログでの調査により発覚)。(1) **Delta-N算出式**: `FnumUtils.h`の`FnumTableType::DeltaN`ケースが実チップの式`delta_n=round(2^16*freq*divide/master)`のうち`divide`(OPNA/OPNB=144)の乗算を丸ごと欠落させていた上、基準周波数に他の型と同じ`masterPitch_`(A440チューニング、既定440Hz)を使っていたが、DeltaNは`Fnum.cpp::CFnumTable::GetDeltaN`(旧FITOM、現在未使用)以来の固定16000Hz基準でなければならず、`CYmDelta`のkNoteOffset/kPitchOrigin定数もその16000Hz基準にキャリブレーションされたものだった。両者の欠落・不一致によりC4(MIDIノート60)のDeltaNが本来約25085相当のところ690程度にしかならず、再生速度が実際の約1/36というかなり遅い速度になっていた。`divide`の乗算を復元し、`freq*(16000/440)`で16000Hz基準に変換してから計算するよう修正。(2) **Start/Endアドレス境界**: `RegMap`に`addrShift`(バイトオフセットをレジスタ値へ変換する際の右シフト量、ymfmの`adpcm_b_channel::address_shift()`相当)を新設。旧実装は全チップに4byte境界(shift=2)を固定で使っていたが、OPNB/OPNBB(YM2610/2610B)は実チップの回路仕様上256byte境界(shift=8)固定であり(OPNA/Y8950は4byte境界で正しい)、OPNB/OPNBBの再生アドレスが実際の配置位置から64倍ズレていた。`AdpcmVoice::startAddr`/`length`の内部表現もビット単位からバイト単位に変更し、`registerVoice()`/`updateVoice()`双方で`addrShift`に基づき変換するよう統一。**(2026-07-24追記)** OPNA ADPCM-Bと同一パッチのはずのOPNB ADPCM-Bが同じ音にならず(ノイズになる)との報告を受け、原因切り分けのため`kOPNB_DeltaT`の`addrShift`値を8→2へ一時的に差し戻して検証したが改善しなかった。`YMEngine/extern/ymfm/src/ymfm_opn.cpp`のchip別コンストラクタ(`ym2608::ym2608()`は`m_adpcm_b(intf)`でaddrshift省略=0、`ym2610::ym2610()`は`m_adpcm_b(intf, 8)`で明示的に8)と`adpcm_b_channel::address_shift()`(`ymfm_adpcm.cpp`、コンストラクタ引数が非0なら無条件にその値を返す実装)を確認し、YM2610/2610Bは256byte境界(shift=8)固定で確定と裏取りできたため8に戻した(2の方が誤り)。8でも症状が残る場合、真因はaddrShift値ではなくadpcm_packer側の実際のデータ整列(pcmbank.jsonの`boundary`フィールドはFITOM_X側で検証に使われない情報欄[`config_schema/pcmbank.schema.json`]のため、実データが本当に256byte境界に整列されているとは限らない)を疑う必要がある。下記「既知の未対応」参照。(3) **センターパン無音**: ADPCM-Bのcontrol2レジスタのbit7/6(pan_left/pan_right)は、実チップ上ステレオ定位だけでなく「出力有効化ビット」を兼ねる(両ビット0だと演算結果が出力に一切加算されない)。`CSoundDevice::noteOn()`はpanDirty(前回値との差分)が立った場合のみ`updatePanpot()`を呼ぶ設計だが、panpotの既定値は0(center)であり、MIDI側が明示的にパンCCを送らない限り「0→0で変化なし」と判定され`updatePanpot()`が一度も呼ばれず、control2がリセット値(両ビットOFF)のまま放置されてセンターパンでは恒久的に無音になっていた。`CYmDelta`のNoteOn処理に、上位のdirtyフラグに関係なく`updatePanpot(ch)`を無条件呼び出しするよう追加(このチップのみ「毎ノートオンで確実に書く」必要があるための個別対応)。(4) **マスタークロック**: `createCAdPcm()`がFM系チップドライバ(`createCOPNA`/`createCOPNB`/`createCOPN2`等)と異なり、Delta-N算出のmasterに`port->getClock()`(実クロック)ではなくサンプルレート`sr`をそのまま渡していた。`port->getClock()`(取得不可時のみ`sr`にフォールバック)を使うよう統一。(1)の式は独立して修正が必要で、クロック取得の是非のみを先に直した際は逆にDeltaNが3〜4という壊滅的な値になり一度差し戻した経緯がある | ✅ | — |
| ADPCM-B(Delta-T)でOPNBが誤ってOPNA用PCMイメージのオフセットテーブルを参照していたバグを修正(2026年7月、上記(153行目)のaddrShift=8修正後もOPNBがノイズになり続けたため、実機のレジスタダンプ比較で発覚)。真因はaddrShiftではなく`PcmBankRegistry`側にあった。`CFITOM::initDevices()`は各PCMデバイスへ割り当てるバンク番号を`FITOMConfig::deviceTypeToVoicePatchType(deviceType)`経由のVoicePatchType一致のみで解決していたが、OPNA用ADPCM-B(`DEVICE_ADPCMB_OPNA`)とOPNB/OPNBB用ADPCM-B(`DEVICE_ADPCMB`)は音色パラメータ形式共有のため同一VoicePatchType(`VOICE_PATCH_ADPCMB`)を意図的に共有しており、`PcmBankRegistry::findBankNoForVoicePatchType()`は「最初に見つかった一致」しか返せないため、プロファイルに`group:"ADPCMB"`のpcm_banksエントリが1つ(OPNA用、32byte境界のオフセットテーブル)しか無い場合、OPNBのADPCM-Bデバイスにも同じOPNA用オフセットテーブルが誤って割り当てられていた(FitomEmuIF/YMEngine側の`pcm_image_catalog.json`はOPNB用に別キー`OPNB_ADPCM-B`で正しいバイナリ[256byte境界]を既にロードしていたため、実際のPCMメモリ内容とFITOM_X側が計算するStart/Endオフセットが完全に食い違っていた)。`PcmBank`に`deviceType`フィールド(`DEVICE_ADPCMB_OPNA`/`DEVICE_ADPCMB`/`DEVICE_ADPCMB_Y8950`等)、`PcmBankRegistry::findBankNoForDeviceType()`(deviceType完全一致、見つからなければ従来のVoicePatchType一致へフォールバック)を新設し、`CFITOM::initDevices()`はこちらを先に試すよう変更。profile.jsonの`pcm_banks[].chip`(任意、"OPNA"/"OPNB"/"OPNBB"/"Y8950")で物理チップ単位にバンクを指定できる(`Config.cpp`の`resolvePcmBankChipDeviceType()`が変換、`config_schema/profile.schema.json`にスキーマ追加)。entries[]のエントリ番号(WS/waveIndex)とサンプル名の対応は物理チップに依らず共通のため、HwBank/SampleZoneBank側の音色パッチ定義自体は引き続きOPNA/OPNBどちらのデバイスにも共通で使い回せる(chipを指定しても「束ね」=パッチ互換性は失われない、offsetテーブルという実装内部の詳細のみがチップ単位に分離される)。ただし`chip`指定時、entries[]からのnamed patch自動合成(`PatchManager::loadPcmBankJson()`)はバンクごとに独立して走るため、同一の名前集合が複数バンク番号の下でパッチピッカーに重複表示される点は既知のトレードオフ(実運用ではdrum_banks[]等の固定prog経由の参照が主でこの重複は表面化しにくい)。**(2026-07-24追記)** 上記修正後、実機ログ(`mergeSpannableDevices`)でOPNA/OPNB/OPNBBのADPCM-Bが`VoicePatchType=0x51`一致で1つの`CSpanDevice`(代表名"OPNA#2-ADPCMB"、3物理チップ・3ch)へ正しく束ねられていることは確認できたが、ユーザーから「束ねられていない(OPNB/OPNBB側が反映されない)」との報告があり再調査。原因は`CMultiDevice::setPcmRegistry()`(`MultiDevice.h`)が、代表デバイス(束ねの先頭、この場合OPNA)のdeviceTypeから解決された1つの`bankNo`を、束ねられた全サブチップ(OPNA/OPNB/OPNBBそれぞれの実`CYmDelta`インスタンス)へ無条件にブロードキャストしていたため、OPNB/OPNBBのサブチップにもOPNA用バンク(bank0)がそのまま伝播し、上記のdeviceType別バンク解決が束ね構成では無効化されていたことだった(単一チップ種のみの束ね[例: SSGを3枚束ねる等]では全サブチップが同じdeviceTypeのため問題化しなかった)。`CMultiDevice::setPcmRegistry()`を、各サブチップについてまず`c->getDeviceType()`(サブチップ自身の実際のdeviceType)で`findBankNoForDeviceType()`を個別に引き直し、見つかった場合はそちらを優先するよう修正(見つからない場合のみ代表デバイス基準の`bankNo`にフォールバック、単一チップ種の束ねとの後方互換を維持)。**(2026-07-24さらに追記)** ポリフォニック発音・束ねは正常に確認できたが、「同一の名前集合が複数バンク番号の下でパッチピッカーに重複表示される」という上記のトレードオフについて質問があったため、これも解消した。`PatchManager::loadPcmBankJson()`/`Config.cpp`のpcm_banksローダーに`offsetsOnly`引数/`pcm_banks[].offsets_only`(任意、既定false)を新設し、trueを指定したバンクはStart/Endオフセットテーブルとしてのみ登録され、named patch自動合成(sampleRegistry()への公開)をスキップするようにした。同一group内でchip違いのバンクを複数併用する場合、代表(通常はchip省略または最初のバンク)以外に`offsets_only: true`を指定すればパッチピッカーの重複表示を避けられる(`config_schema/profile.schema.json`にスキーマ追加) | ✅ | `chip-driver-architecture.md` |
| `CAdPcm2610A`(ADPCM-A、YM2610/2610B)が束ね(`CSpanDevice`)構成で後半チップに一切ボイス割り当てされないバグを修正(2026年7月、OPNB+OPNBBの2物理チップ・12ch束ねで「前半6chしか使われず後半6chに全く割り当てが行かない」との報告で発覚)。`CAdPcm2610A::queryCh(owner, patch, mode)`が、渡された`mode`引数を無視して常に`CSoundDevice::queryCh(owner, patch, 0)`(mode=0=強制奪取あり)を呼ぶ実装になっていた(コメント無し、初期実装[2026年7月3日]からの潜在バグ、他のチップドライバ[OPN/OPM/PSG系]は全て`mode`をそのまま転送しており今回が唯一の例外だった)。`CSpanDevice::allocCh()`はmode=1(奪取なし)→0(奪取あり)の順で全サブチップを試すことで「まだ空きがあるチップを優先し、全チップが本当に埋まって初めて奪取に回る」設計だが、`CAdPcm2610A`はmode=1で呼ばれても内部で強制的にmode=0扱いにしてしまうため、1台目(chips_[0])が常に(既存ノートを奪取してでも)空きchを返してしまい、`allocCh()`のループが1台目で必ず成功して2台目(chips_[1])に到達することがなかった。`mode`を無視するこの一見無意味な(base実装と同じ処理をmodeだけ固定して呼び直すだけの)オーバーライド自体を削除し、`CSoundDevice::queryCh()`をそのまま継承させることで修正(継承元は`mode`を正しく反映する実装のため、削除するだけで直る)。**(2026-07-24追記)** 修正後もユーザー報告で改善しないとのことだったため、`CSpanDevice::allocCh()`が全サブチップ失敗時にWARNログ(`no channel available across N spanned chip(s)`)を出すよう追加し、さらに割り当て成功時にどのサブチップ・どのmodeが選ばれたかを記録する一時DEBUGログも追加して再テストしたところ、実機ログで「1台目の6chが埋まった時点で毎回mode=1のままchip[1]へ正しくスピルオーバーしており、mode=0(強制奪取)は1件も発生していない」ことを確認できた。修正は正しく機能していることが確定したため、一時DEBUGログは削除し、恒久的に有用なWARNログのみ残した(調査当初「6ポリのまま」と見えたのは、直前のビルドが未反映だった可能性が高い) | ✅ | — |
| `CSpanDevice::allocCh()`のチップ試行順序をローテーション化(2026年8月、ユーザー指摘)。GM準拠のリズムパートはほとんどのドラムノートにゲートタイムの概念が無く(規格上シーケンサーが送るノートオンのゲートタイムはごく短い)、ノートオフ受信後も実際の発音はすぐには止まらない(クラッシュシンバル等の長い減衰)。従来の`allocCh()`は常に`chips_[0]`から空き/Releasingのchを探し、見つかり次第即確定していたため、1台目に(software上は`isReleasing()==true`だが実際にはまだ減衰中の)chが1つでもある限り常に1台目だけが選ばれ続け、2台目以降のチップにほとんど到達しなかった(2台目が使われるのは1台目が"真に"全ch Running状態の瞬間のみ)。これは同一物理chの再利用サイクルが早まり、まだ減衰中のノートが不用意にチョークされるリスクを高める。実際の発音時間(ROM解析による減衰時間テーブル等)まで管理するのは「やりすぎ」という判断のもと、代わりに`nextChipIdx_`(割り当て成功のたびに1つ進める)でチップの試行開始位置を毎回ローテーションし、束ねた全チップに均等にラウンドロビンさせることで同一chの再利用頻度を下げる方針とした。各チップ固有の`queryCh`制約(OPMノイズch7固定等)には手を加えず、チップの試行順序だけを変えるため既存の制約は保たれる | ✅ | `chip-driver-architecture.md` |
| ドラムノートのチョークグループ(`DrumPatch::chokeGroups`)が事実上まったく機能していなかった不具合を修正し、実現方式を再設計(2026年8月、ユーザー指摘)。原因は2点。①`CRhythmCh`の発音記録(`NoteSlots::stopAll()`)がゲートタイム満了時・NoteOff時にスロットを完全クリアしていたため、チョークが走る頃には停止対象の記録が消えており完全なno-opになっていた(ドラムのゲートタイムはごく短く、ゲート満了時点ではチップ側でまだ音が鳴っているのが普通)。②停止手段が`noteOff()`だったため、FM系ではリリースレート次第で余韻が残り、さらにADPCM-A(`CAdPcm2610A`)とOPNA内蔵リズム(`COPNARhythm`)は`updateKey(ch,false)`が意図的にno-op(ワンショットのドラム音は最後まで鳴らすほうが自然)であるため一切音が止まらなかった。`LayerSlot`を「発音管理としてのアクティブ状態(`active`)」と「直前の発音への物理チャンネル参照(`dev`/`devCh`/`noteOnSeq`)」に分離し、参照はノートオフ/ゲート満了後も保持するよう変更。参照は2用途: (a)同一ノート再打鍵時は`allocCh()`ではなく`assignCh(devCh,...)`で同じ物理chを取り直し、`assignCh()`のKey Off送出+各ドライバの`wasReleasing`事前ダンプによる物理的リトリガーで前の音を自然に切る(強制ダンプ不要、旧`fixed_ch`方式の「1ノート=1ボイス」が復活)、(b)チョークグループは別ノート=別ch のため`forceDamp()`による明示的な強制ダンプ。参照の有効性は`isChOwnedBy()`だけでなく`ChState::noteOnSeq`の一致でも判定する(`CRhythmCh`は128ノートをまたぐ単一の`IMidiCh`のため、`isChOwnedBy()`だけでは同じ`CRhythmCh`の別ドラムノートへ再配分された`devCh`も所有と判定され、無関係なドラム音を誤って止めて/奪ってしまう)。強制ダンプのインターフェースは新設せず、CC#120用に既存の`ISoundDevice::forceDamp()`をそのまま使い、未実装だった`CAdPcm2610A`(reg`$00` bit7=DM)と`COPNARhythm`(reg`$10` bit7)にoverrideを追加。両者は`updateKey(ch,true)`でもキーオン前にダンプを書く(NoteOffで止まらない以上、同一ch再割り当て時に前のサンプルがまだ再生中の可能性が常にあり、これが無いと(a)が確実な物理的リトリガーにならない。FM系の`wasReleasing`事前ダンプと同じ役割だが純粋な停止指示で冪等なため無条件)。あわせて`CRhythmCh::allSoundOff()`(CC#120)を実装(既定実装の`allNoteOff()`では上記2デバイスが鳴り止まなかった)。サンプルの実再生長を解析・管理する方式は「やりすぎ」との判断で採らず、その帰結としてチョーク可能な時間窓は`kReleasingHoldMs`(2秒、経過後は`ChState`が`Empty`に落ち`isChOwnedBy()`がfalseになる)に限られる点を既知の制限として文書化 | ✅ | `patch-structure-design.md` |
| レジスタダンプモニターでOPNB(YM2610無印)/2610B(YM2610B)の高位ポート(0x100以降、ADPCM-A/ADPCM-B等が配置される)が表示されない不具合修正(2026年7月)。上記(147行目)の「port2が無くてもkDevMapの既知のレジスタ空間サイズが0x100超なら一括読み出し」という修正時に、`CFITOM.cpp`の`kDevMap`へ`DEVICE_OPNB`/`DEVICE_2610B`のエントリを追加し忘れていたため、`getDeviceRegSize()`が0を返し`PhysicalChipInfo::dumpSize`が既定の0x100のまま(0x000-0x0FFのみ)になっていたのが原因。両デバイスともCOPNA同様port2/OffsetPort経由で0x100-0x1FFを使うため、OPNA/OPN2と同じ`{VOICE_TYPE_FM4, VOICE_GROUP_OPNA, 0x200}`を追加した | ✅ | — |
| OPL4のAWM部が上記(153行目)のポートアドレス修正後もなお発音しない不具合を修正(2026年7月、ユーザーからの続報で発覚)。真因はYMF278Bの`NEW2`ビット(レジスタ`0x105`のbit1)が一度も立てられていなかったこと。ymfmの`ymf278b::write_data_pcm()`(`YMEngine/extern/ymfm/src/ymfm_opl.cpp`)は`new2flag()==0`の間、AWM/PCM側レジスタへの書き込みを黙って無視する仕様だが、`COPL3::init()`/`COPL3_2::init()`(`OPL_new.cpp`、OPL4のFM-4OP/FM-2OP部が兼用)はどちらも`0x105`に`0x01`(`NEW1`のみ)しか書いておらず`NEW2`が有効化されていなかった(ポートアドレス修正[153行目]によりAWM部のレジスタ書き込み自体は正しい宛先=3バンク目に届くようになっていたが、YMF278B側がその書き込みを`NEW2`未設定を理由に受理していなかった)。加えて、composite展開順(FM-4OP→FM-2OP→AWM)により`COPL3_2::init()`が`COPL3::init()`より後に実行され`0x105`を再度`0x01`で上書きするため、どちらか一方のみ修正しても他方が打ち消してしまう点に注意が必要だった。両`init()`とも`setReg(0x105/chip2_->setReg(0x05), 0x03, true)`(`NEW1|NEW2`)に変更して解消。`new2flag()`は`ymfm_opl.h`の`IsOpl4Plus`(Revision>=4、`ymf278b`のみtrue)でゲートされているため、単体OPL3(`ymfm::ymf262`、Revision3)ではbit1を立てても`new2flag()`は常に0を返し無視されるだけで無害 | ✅ | — |
| 外部パッチエディタ起動機能、実機での初回動作確認により発覚したプロファイルパス解決バグを修正(2026年7月)。`apps/fitom_gui/main.cpp`の`launchPatchEditorForChannel()`がパッチエディタ子プロセスへ渡す`profilePath`/`hwBankFile`は、プロファイルを相対パスで指定していた場合(`FITOMConfig::loadProfile()`のbaseDir=`path.parent_path()`も相対のまま伝播するため)fitom_gui自身のCWD基準の相対パス文字列のままだった。一方`launchProcess()`は子プロセスの作業ディレクトリをパッチエディタの実行ファイル自身のディレクトリに固定するため、fitom_gui起動時のCWDと異なる場所を基準に解決されてしまい「cannot open file」でプロファイル読み込みに失敗していた(パッチエディタ側は正しく動作しており、原因はFITOM_X側にあった)。子プロセスへ渡す直前に`fs::absolute()`で絶対パス化するよう修正 | ✅ | — |
| 内部用MIDIパイプ(`fitom_midi_pipe`)を、プロファイル設定(`midi_backend`/`midi_inputs`)から完全に独立させ、`-DFITOM_BUILD_BACKEND_MIDI_PIPE=ON`でビルドすれば常時・無条件で有効化されるよう変更(2026年7月、外部パッチエディタとの併用確認中に「実機MIDIキーボードとの同時併用ができず不便」との指摘で着手)。従来`FITOMBridge`/`fitom_cli`はMIDIバックエンドDLLをアプリ全体で1つしか同時保持できず、実機MIDIキーボード用のrtmidiバックエンドとfitom_midi_pipeが同一プロファイル内で併用できない制約があった(`IMidiPlugin.h`契約自体ではなく、アプリ層が「バックエンドDLLは1つだけ」と決め打ちしていたことに起因)。`CFITOM`に既存の`MAX_MPUS`(4)本のMPUとは完全に独立した内部パイプ専用の`MidiProcessor`+16ch(`internalPipeChannels_`/`internalPipeProcessor_`、`getInternalPipeProcessor()`)を新設し、`init()`で常に構築、`timerCallback`/`pollingCallback`/`midiClockCallback`/`allNoteOff`/`resetAllCtrl`/`setScaleTuning`の既存`MAX_MPUS`ループの直後にも組み込んだ。`FITOMBridge::initInternalMidiPipe()`(および`apps/fitom_cli`側の同等ロジック)が、プロファイルを一切参照せず`fitom_midi_pipe`のDLLファイル存在有無のみで無条件にロード・オープンする(存在しない場合は非致命的にスキップ)。`MAX_MPUS`/`getMpuCount()`自体は変更していないため、GUIのMIDIモニター・MIDIポート設定ダイアログ等、既存のMPU前提UIは無改修のまま(内部パイプは意図的にこれらに表示されない)。PowerShellの`NamedPipeClientStream`による疎通確認(チャンネル割り当て通知の受信→Program Change/Note On/Offの送信→ログでの反映確認)で、rtmidiバックエンドのロード失敗とは無関係に内部パイプが独立して機能することを確認済み | ✅ | `plugin-midi-pipe.md` |
| 内部用MIDIパイプを常時有効化した直後、実機で「パッチエディタを閉じた後FITOM_Xの終了操作がハングする」不具合が発覚し修正(2026年7月)。真因は`backends/midi_pipe/src/MidiPipe.cpp`の`~MidiInDevice()`(パイプの後始末処理、以前からの既存コード)が、`ConnectNamedPipe()`でブロック中の`acceptThread`を起こす手段として別スレッドからの`CloseHandle()`のみに頼っていたこと。Windowsではブロッキングモードのハンドルに対し、同期I/O呼び出し中のスレッドとは別のスレッドが`CloseHandle()`しても、その呼び出しが確実に解除される保証がない(既知の制約)。パイプが常時有効化されたことで、パッチエディタ未接続時でも起動直後からacceptThreadが常時この待機状態になり、終了時に高確率でハングするようになって初めて表面化した(以前はプロファイル側で明示的に有効化しない限りこの経路自体が実行されなかったため潜在化していた)。`CancelSynchronousIo()`(スレッド単位で保留中の同期I/Oをキャンセルできる、Vista以降のAPI)を`CloseHandle()`の前に追加し、acceptThread・各ワーカースレッド(パッチエディタ接続が生きたまま終了処理に入った場合の`ReadFile()`待ち)双方に適用するよう修正。`tests/test_midi_pipe.cpp`(`FITOM_BUILD_BACKEND_MIDI_PIPE=ON`時のみビルド、それ以外は既存テストへの影響ゼロ)を新設し、パッチエディタ未接続のままポートを閉じてもハングしないことを別スレッド+タイムアウト付きfutureで検証する回帰テストとして追加(修正前のコードに対して意図的に実行し、5秒でタイムアウト・テスト失敗することを確認したうえで、修正適用後にPASSすることも確認済み) | ✅ | `plugin-midi-pipe.md` |
| 外部パッチエディタ(`FITOM_patch_editor`)側の起動引数仕様変更(D-039/D-040、`../FITOM_patch_editor/docs/DESIGN.md`参照)に追従(2026年7月)。パッチエディタがhwpatch(デバイス)編集画面専用の3引数(`<profile> <hwbank-file> <prog>`)から、種別を挟んだ4引数(`<profile> <kind> <bank-file> <prog>`、`kind`="device"/"layered"等)へ変更されたため、FITOM_X側もチャンネルの現在のパッチ種別に応じて適切な`kind`で起動するよう追従した。`FITOMBridge::resolveChannelHwPatch()`のシグネチャに`outKind`を追加し、通常モード(bankSelMSB==0、レイヤードパッチ/PatchBank選択)では`kind="layered"`+`*.patchbank.json`+レイヤードPatchのprog番号を、直接モード(bankSelMSB!=0)では従来通り`kind="device"`+`*.hwbank.json`+HW prog番号を返す。以前は通常モードでも`pm.resolve()`で先頭ToneLayerのHwPatchまで解決してしまい「レイヤードパッチ経由だった」という情報が呼び出し元に渡る前に失われ、常にデバイスパッチ編集画面が開いてしまう不具合があったが(パッチエディタ側の調査で発覚、D-039の`FITOM_X本体側`部分)、通常モードでは`pm.resolve()`を呼ばず`pm.findPatchBank(bankNoLsb)`のfilenameとprogNoをそのまま返すよう変更したことで解消した。`apps/fitom_gui/main.cpp`の`launchPatchEditorForChannel()`も4引数の起動コマンドラインを組み立てるよう追従。performance/drum種別は「チャンネルの現在のパッチ」になることが無いため(パフォーマンスパッチは常にDevice/Layeredから参照される側、ドラムチャンネルは`isRhythm()`で従来通り除外)、今回のFITOM_X側の対応範囲には含まれない | ✅ | — |
| `VoiceProcessor::onVolumeChange()`未使用バグの修正(2026年7月)。発音中のCC7(Volume)/CC11(Expression)変更が`effectiveTL()`経由の音量計算へ反映されない問題(下記「既知の未対応・将来課題」に記載していたもの)を修正。原因は`onVolumeChange()`自体がどこからも呼ばれていなかったこと。`CSoundDevice::setVolume()`/`setExpression()`が値変更を検出した時点で`ChState::proc.onVolumeChange()`を呼ぶよう変更した。`onVolumeChange()`はTL計算に`voice.swOp[].VTL`(ベロシティ/エクスプレッション感度)を必要とするが、従来`assignCh()`時にのみ一時的な`FmVoice`を組み立てて`onNoteOn()`へ渡すだけで、SwPatch自体はどこにも保持していなかった。そのため`ChState`に`swPatch`(値コピー)を新設して`assignCh()`でキャッシュし、`CSoundDevice::buildVoiceForCh()`(新設ヘルパー、hwPatch/samplePatch/swPatchキャッシュから`FmVoice`を再構築)経由で`assignCh()`/`setVolume()`/`setExpression()`が共通利用するよう整理した。副次効果として、ボイススチールでチャンネルが再利用された際、`assignCh()`内の`onNoteOn()`が新ノートの前に残っていた旧CC7/CC11値で最初のTLを計算してしまい、直後の`setVolume()`/`setExpression()`(旧実装ではdirtyフラグを立てるだけ)がそれを訂正できていなかった、新規ノート発音直後の初期音量が誤る不具合も併せて修正された(CC変更が無い通常の連続ノートオンでは元々問題なし) | ✅ | — |
| pcmbank/samplezonebankへのパフォーマンスパッチ(SwPatch)紐づけ(2026年7月新設)。`SampleZone`に`swBank`/`swProg`(`HwPatch`と同じ規約)を追加し、`*.samplezonebank.json`の`zones[].sw_bank/sw_prog`、`*.pcmbank.json`の新設`swpatches[]`(`entries[]`とentry_noで対応)の2経路で指定できる。`DrumNote::swBank/swProg`による上書きも既存のHwPatchと同じ優先順位で効く。解決はNoteOn時点で`SampleZonePatch::resolveZone(note, vel)`(COPL4AWM/CAdPcmBaseに重複していたゾーン線形探索をここへ統一)により実際に一致するゾーンを求めてから行う。**実装過程で2件の既存バグを発見・修正**: (1) ゾーン探索(`updateVoice()`)が`assignCh()`内部で`s.lastNote`が正しいノートに更新される前(`setNoteFine()`は`assignCh()`より後に呼ばれる)に走ってしまい、常にチャンネル再利用時の1つ前のノート(初回は`0xFF`)でゾーンが決まっていた潜在バグ(`setNoteFine()`が`update`かつ`s.samplePatch`ありの場合`updateVoice()`を呼び直すよう修正、4チップ共通)。(2) `CSoundDevice::assignCh()`の`samplePatch`分岐が`VoiceProcessor::onNoteOn()`を一切呼んでいなかったため、音量計算が`effectiveTL(0)`のみに依存する設計のCYmDelta(ADPCM-B)/CAdPcmZ280(PCMD8)は、MIDI Volume/Expression/Velocityを無視して常に最大音量で再生されていた(本機能とは無関係の既存バグ)。`CSoundDevice`に`usesVoiceProcessorForSamplePatch()`/`sampleVoiceProcessorVolume()`の2仮想関数を新設し、該当チップのみ`onNoteOn()`を呼ぶよう修正、`updateTL()`(従来no-op)も`updateVolExp()`を呼ぶよう変更してトレモロLFOの継続反映にも対応した。CAdPcm2610A(ADPCM-A)はピッチ制御が無くビブラート/fine_transposeが原理的に不可能なため対象外としつつ、実機のVol/Exp-Vel分離設計(vol=総合音量レジスタ、exp×vel=チャンネルレジスタ)を活かして`onNoteOn()`へvol=127(中立値)を渡すことで、ベロシティ感度(VTL)/トレモロのみ安全に追加した(総合音量レジスタ・複数MIDI CH間の競合という既存の制約には触れていない)。COPL4AWM(AWM)は実機がLFO/VIBレジスタをデバイス機能として持ち波形バイナリ側の設定と整合させる設計が別途必要なため、意図的に対象外のまま(`onNoteOn`を呼ばない)とした | ✅ | `patch-structure-design.md`, `voice-parameter-reference.md` |
| SF2直行パス(fluidsynth統合、`docs/sf2-fluidsynth-integration.md`参照)のFITOM_X本体側実装(2026年7月新設)。新設`Sf2BankRegistry`(`core/include(src)/fitom/Sf2BankRegistry.{h,cpp}`)が`banks.sf2_banks[]`を1回のパースからCC#32解決用マップと`devices[]`へ渡す`soundfonts`一覧(重複除去・初出順)の両方を導出する。`FITOMConfig::buildFromProfile()`はdevices[]構築より前にこれを構築し、`chip:"SF2"`の`devices[]`エントリのparams_jsonへ`soundfonts`を自動注入する(`resolveChipDeviceId("SF2")`は`DEVICE_NONE`へ明示的に解決し、既存の「未知チップ」警告を出さない)。トップレベル`sf2_channel_windows[]`も読み込み・検証(fluidsynth_chan/(mpu,ch)の重複、範囲外値、16エントリ超過を起動時エラーに)する。プロファイル記述レベルで`chip=="SF2"`が複数ある場合・`sf2_banks`/`sf2_channel_windows`が設定されているのに対応する`devices[]`が無い場合も起動時エラーとする。`CFITOM::initDevices()`は`chip:"SF2"`の`DEVICE_NONE`スキップを他の「未知チップ」skipと区別し(警告を出さず)、そのIPortを`sf2Port_`として記録する。`MidiProcessor`に`mpuIndex_`を持たせ、`processMessage()`が窓(MPU×ch)に含まれるメッセージを`CInstCh`/`CRhythmCh`へのディスパッチより前に`CFITOM::routeSf2ChannelMessage()`へ振り分ける(CC#0破棄、CC#32→`sf2_banks`解決のキャッシュ、有効な解決が無い間はプログラムチェンジも読み捨て、解決済みならプログラムチェンジ受信時にプライベートSysEx sub-cmd 0x05(`F0 00 48 01 05 <chan> <soundfont_index> <bank_msb> <bank_lsb> <prog> F7`)を`HWPlugin_WriteBlock`経由で送出、それ以外の通常メッセージはchanを付け替えてそのまま転送)。プライベートSysEx sub-cmd 0x04(`F0 00 48 01 04 <mpu> <ch> <fluidsynth_chan\|0x7F> F7`)による窓の動的割り当て・解除(`CFITOM::setSf2ChannelWindow()`、fluidsynth_chanの重複割り当ては要求ごと拒否)も実装。マスターボリューム/マスターピッチの変更(`CFITOM::setMasterVolume()`/`setMasterPitch()`)は、新規プロトコルを設計せず既存のGM2 Universal Realtime SysEx(マスターボリューム`04 01`・マスターファインチューニング`04 03`)をそのままSF2デバイスへ転送する。`tests/test_config.cpp`に`Sf2BankRegistry`の解決・重複除去・不正エントリ、`sf2_banks`/`sf2_channel_windows`の各種バリデーション(起動時エラーになるケース含む)のユニットテストを追加。**このリポジトリのスコープ外(未着手)**: `fluid_synth_t`を実際にリンクするhwifプラグイン本体(仮称`FitomSf2IF`、別リポジトリ)、`sf2_channel_windows`編集用のGUI専用ダイアログ、MIDIモニターのDevice/Fnumber列でのSF2直行パス表示。`FitomSf2IF`が存在しない現状は`sf2Port_`が常に`nullptr`のため、窓に含まれるメッセージは単純に読み捨てられる(実害はないが実際の発音確認は`FitomSf2IF`の実装後になる) | ✅ (FITOM_X本体側) | `sf2-fluidsynth-integration.md`, `manuals/midi-message-reference.md` |
| ADPCM-A/ADPCM-BでExpression/Velocityの極性が反転していたバグを修正(2026年7月、上記pcmbank/samplezonebankへのSwPatch紐づけをユーザーが実機確認した結果、報告され発覚)。`VoiceProcessor::effectiveTL()`はラウドネス空間(0=無音,127=最大音量、`OPN_new.cpp`の`effTLToReg()`コメント参照)を返す設計だが、`CAdPcm2610A`(ADPCM-A)/`CYmDelta`(ADPCM-B)の`updateVolExp()`が、この値を`127-effectiveTL(0)`で不要に反転させてから使っていたため、Expression/Velocityの効きが逆転していた。ADPCM-Aは元々`calcVolExpVel()`の戻り値(同じラウドネス空間の規約)をそのまま渡していたところへ、上記コミットで`effectiveTL()`に置き換えた際に反転を誤って追加してしまっていた。ADPCM-Bは同コミットでは一切変更していない既存コードだが、同種の反転が元から入っており、`onNoteOn()`を呼ぶよう修正したことで`effectiveTL()`が実際に機能するようになり、初めて症状として顕在化した。両者とも反転を除去し、`effectiveTL()`を`calcVolExpVel()`と同じラウドネス空間の値としてそのまま使うよう修正 | ✅ | — |
| モノ+レガート(CC#68)チャンネルで、最後の鍵盤を離してもノートオフが永久に効かないバグを修正(2026年7月、レジスタダンプモニターで演奏停止後もF-number/TLレジスタが更新され続けると報告され発覚)。`CInstCh::noteOff()`が初期実装(Initial Commit)から`if (mono_ && legato_) return;`という無条件の早期returnを持っており、レガートモード中はnoteOff()が実質何もしない実装になっていた。レガート中に新しいノートオンが前の音を奪う場合は既存の`noteOn()`側`mono_ && timbres_>0`分岐が`leaveNote()`/`enterNote()`で`notes_[]`のnote番号を新しいノートへ差し替えるため、奪われた古いノート番号への遅延noteOff()は通常のnote番号一致判定で自然に無視される(=この早期returnが無くても問題は起きない)。一方、新しいノートオンで奪われず単に鍵盤が離された最後の1音は、この早期returnにより`s.release()`/`proc.onNoteOff()`が一切呼ばれずChStateがRunning状態のまま取り残され、CC#1ビブラート等のソフトウェアLFOも含め鳴り続けていた。当該早期returnを削除し、通常のnoteOff処理(note番号一致→release)をレガートモードでもそのまま通すよう修正 | ✅ | `midi-implementation-status.md` |
| レイヤードパッチ(ToneLayer複数)でNoteOffが一部レイヤーにしか効かないバグを修正(2026年7月、上記のモノ+レガート修正後も「モノフォニックにしていないのに演奏停止後もレジスタが更新され続ける」と再報告され、レジスタダンプモニターの更新され続けるアドレス(OPN2/OPNAの1チップ分、ポート1・ポート2双方の複数ch)をヒントに再調査して発覚)。`CInstCh::noteOn()`はアクティブな`ToneLayer`の数だけ`notes_[]`へ同じnote番号のエントリを複数(レイヤーごとに異なるdev/devCh)生成するが、`noteOff(note)`は`notes_[]`を先頭から走査してnote番号が一致した最初の1件を解放した時点で`break`していたため、2レイヤー目以降の同じnote番号のエントリが解放されず、対応するチャンネルがRunning状態のまま(CC#1ビブラート等のソフトウェアLFOも含め)永久に鳴り続けていた。`stealOldestNoteIfNeeded()`(ボイス数上限によるスティール)は同じ「同一noteが複数レイヤーに跨る」前提を`break`せず全スロット走査で正しく扱っており、`noteOff()`もこれに揃えてnote一致時の`break`を削除し、全レイヤー分のエントリを解放するよう修正した。レイヤードパッチを使わない(1レイヤーのみの)構成では従来通り1件しか一致しないため影響なし | ✅ | `midi-implementation-status.md` |
| forceSteal(Running chの強制奪取)時、同一ownerだとKey Offが抜け落ちるバグを修正(2026年7月、上記2件を修正してもなお「モノフォニックにもレイヤードパッチにもしていない、高速に連続でノートオンが発生するトラック[ベースライン等]でノートオンを取りこぼしている[または直前のノートオフが効いていない]ように聞こえる、Fnumは更新されている」との報告、および「レジスタ0xA0-0xA2の値が一定値になると更新が停止する」という追加観察をヒントに再調査して発覚)。`CSoundDevice::assignCh()`は「他のownerが使用中のchを奪う場合のみ事前にnoteOff()する」(`s.owner != owner`時のみ)という条件になっていたが、`findBestCh()`のscore=1(空き/Releasingが1つも無い場合の最終手段、Running chのうちnoteOnAge最大[最古]を強制奪取)は奪う対象をowner一致かどうかで区別しないため、高速連打でデバイスの全ch(他パートと共有するチップ構成等)を使い切っている状況では、選ばれた最古のRunning chが「自分自身[同一owner]の直前のノート」であるケースが普通に起こりうる。この場合noteOff()がスキップされ、Key Off(OPN系ならレジスタ0x28)が一度も書かれないまま次のKey Onだけが書かれていた。OPN系のKey On/Offはエッジトリガ(0→1でアタック開始)のため、Key Offを挟まずKey Onだけ再度書いてもエンベロープは再アタックせず、Fnumだけが新しいノートの値に変わって音程だけが滑らかに(ポルタメントのように)変化して聞こえる(これが「ノートオンの取りこぼし」の正体であり、「Fnumが新しいノートの値へ収束してから更新が止まる」という観察とも整合する)。`assignCh()`の条件を`s.isRunning()`のみへ変更し、ownerの異同に関わらずRunning chを奪う場合は必ずnoteOff()するよう修正(Releasing chの再利用は元々noteOff()済みで無害なため対象外のまま)。`tests/test_soundevice_freq.cpp`に、同一owner+patchでデバイス全chを埋めた状態からさらにallocCh()した場合、奪われたchのレジスタ0x28へKey Off(0x00)がKey On(0xF0)より前に書かれることを検証する回帰テストを追加(修正前のコードに対して実行しKey Offが一切書かれず失敗することを確認した上で、修正適用後にPASSすることも確認済み) | ✅ | `midi-implementation-status.md` |
| **(2026-07-28追記)** `wasReleasing`修正自体は正しいバグ修正だが、これが「高速なベースラインでアタックが再トリガーされず音程だけ滑らかに変わって聞こえる」症状の原因ではないと判明。修正適用後の実測ログで`wasReleasing=true`(RR強制ダンプが発動)になっていることを確認できたにもかかわらず症状が再現し続けたため、ユーザーからの指摘(音源設計の知見)で「エンベロープがリリース中の残存レベルからノートオンした場合、そこから急激にアタックへ向かうためアタックが強く聞こえることはあっても、抜けて(無音に)聞こえる現象にはならない」ことが分かり、リリース残存レベル説を撤回した。さらに、RR強制ダンプの実装自体にも設計上の疑問(RRは今後の減衰の「速さ」を決めるだけで現在のレベルを即座には下げないため、ダンプ直後に間を置かずKey Onしても実質的な効果が無いのではないか)が浮上したが、上記の理由でこの経路自体が今回の症状の原因ではないと判断し、これ以上の追求はユーザーの判断で一旦保留とした。次の切り分けとして、FITOM_X側で観測できる最下層(`HWPort::write()`、実際にプラグインDLL[`HWPlugin_Write`]へ渡す直前)にもタイミングログを追加し、CSoundDevice層(setReg呼び出し)とHWPort層(実際のプラグイン呼び出し)の両方のログを突き合わせることで、「ノートオンが抜けた」際に実際にKey On書き込みがプラグイン境界まで届いているか(=FITOM_X側の問題)、届いているのに音が鳴らないか(=エミュレータ/プラグイン側の問題)を切り分けられるようにした | 🚧 調査中 | `midi-implementation-status.md` |
| **(2026-07-28さらに追記)** 上記`HWPort::write()`ログを実機で採取した結果、「3音目が抜けた」ケースで、`CInstCh::noteOn()`のエントリログと、その直後のレイヤー処理ログ(`stealOldestNoteIfNeeded()`呼び出しを挟む区間)との間に、本来この時点ではまだ発生しないはずの謎のレジスタ書き込み(`0x8c`=キャリアOPのSL/RR、直前に鳴っていた音の通常値と同じ)が挟まっていることを発見。これが同一スレッド内の別ロジックによるものか、別スレッド(MIDI受信スレッドとタイマースレッド間等)からのレースによるものかを切り分けるため、`Log::init()`のログフォーマットにスレッドID(`boost::log`の`ThreadID`属性、`log::attributes::current_thread_id()`を明示登録)を追加した(`fitom_gui`は`FITOMBridge.cpp`経由で`Log::init()`を呼んでおり今回の変更が反映される。`fitom_cli`も同様。テストバイナリは`Log::init()`を呼んでいないため従来通りboost::log既定フォーマット[スレッドID表示あり]のまま)。この情報を使い、当該の謎の書き込みがMIDIスレッドと同一スレッドから来ているか(=ロジック上の何らかの見落とし)、別スレッドから来ているか(=スレッド競合)を次の実機ログで確認する予定 | 🚧 調査中 | `midi-implementation-status.md` |
| **(2026-07-28さらに追記、ノートオン抜け調査の決着)** スレッドID付きログを実機で採取した結果、全ての行が同一スレッドから出ていることを確認(スレッド競合は排除)。また「謎の書き込み」も、`updateVoice()`が書く通常のRR値が`setReg()`の「直前と同じ値ならスキップ」最適化により、直前の状態(RRダンプ後かどうか)次第でログに出たり出なかったりしていただけと判明(実害なし)。決定的だったのは、note=33/32/31/30の4音全てでレジスタ書き込みシーケンスが構造的に完全同一(Fnum値以外一言一句同じ)であるにもかかわらず、聴感上dropするのはそのうち1音だけだったこと。**同一入力に対しFITOM_X側の出力(書き込み内容・順序・タイミング)が完全に同一なのに結果が安定しない**ことから、原因はFITOM_X側ではなく受け取り側にあると判断。ユーザーが音源エンジンをYMFMEngineからFmGenEngineに切り替えたところノートオン抜けが解消したため、ノートオン抜けについては**FITOM_X側の疑いが完全に晴れ、原因はYMFMEngine側(別リポジトリ、このリポジトリのスコープ外)と結論して調査終了**とした。今回の調査で見つかった以下2件のFITOM_X側の実バグ(forceSteal時のKey Off抜け、wasReleasing常時false)は、ノートオン抜けの原因ではなかったが、独立した正当な修正のためそのまま維持する | ✅ (原因はFITOM_X外と判明、調査終了) | `midi-implementation-status.md` |
| **ノートオフ後にレジスタが更新され続ける問題の真因を特定・修正(2026年7月、本件のスタートから続いていた最重要バグ)**。上記のノートオン抜け調査とは完全に独立した別問題として並行調査していたところ、`apps/fitom_gui/main.cpp`のメインループで、コアのタイマーコールバック(`FITOMBridge::onTimer()`→`CFITOM::timerCallback()`)を**GUIのレンダーループから毎フレーム1回**(`bridge.onTimer(1)`)呼んでいたことが判明。`CSoundDevice::timerCallback()`のreleaseTimer減算処理、`VoiceProcessor`のソフトウェアLFO(`LfoControl::tick()`)、ポルタメント(`PortaCtrl::update()`)等、コア全体の時間ベースの処理は「1回の呼び出し=1ms経過」を前提に設計されている(`kReleasingHoldMs=2000`は2000tick=2000ms=2秒のつもり、LFOの周期テーブル`rateToTicks()`も同様)。ところがGUIの実際のフレームレート(~60fps、1フレーム≈16.7ms)でこれを毎フレーム1回しか呼ばないと、**実時間ベースで見た場合、全ての時間ベース処理が約16.7倍間延びする**(kReleasingHoldMs=2000tickは本来2秒のはずが、実時間では2000フレーム分≈33秒かかる)。この結果、ノートオフ後もソフトウェアLFO(ビブラート/トレモロ)がReleasingフェーズの間(実時間で30秒以上)動き続け、Fnum/TLレジスタが更新され続けているように見えていた。原因は`FITOMBridge.h`の`onTimer()`宣言コメント「MFCタイマーからonTimer()を呼ぶため不要」という、現行のDear ImGuiベースのfitom_guiには当てはまらない古い前提(おそらく旧GUI実装からの移植時の記述が残っていた)。`apps/fitom_cli`は最初から`CFITOM::startTimerThread()`(専用の1msスレッド、`std::this_thread::sleep_until`で正確な間隔を保証)を使っており、この問題の影響を受けていなかった。`FITOMBridge`に`startTimerThread()`/`stopTimerThread()`(`CFITOM`の同名メソッドへの薄いラッパー)を新設し、`apps/fitom_gui/main.cpp`をfitom_cliと同じ「起動後に一度`startTimerThread()`を呼び、終了直前に`stopTimerThread()`を呼ぶ」方式に変更。レンダーループ内の毎フレーム`onTimer(1)`呼び出しは削除した。`onTimer()`自体は後方互換のため残す(将来的にMFC等の外部タイマーから呼ぶユースケースが復活した場合のため)。**ユーザー実機で、ノートオフから約2秒でソフトウェアLFOが収束しレジスタ更新が止まることを確認済み** | ✅ (実機確認済み) | — |
| 上記一連の調査で追加していた調査用デバッグログ(`CInstCh::noteOn()`/`noteOff()`・`CSoundDevice::noteOn()`/`noteOff()`・`timerCallback()`のReleasing→Empty遷移・`HWPort::write()`の各`FITOM_LOG_DEBUG`、および`Log::init()`のログフォーマットへのスレッドID追加)は、目的(本項目一連のバグの原因特定)を達成したため、原因確定後に全て削除した。恒久的な修正(`assignCh()`のisRunning()条件、`ChState::assign()`/`run()`のwasReleasing、`CInstCh::noteOff()`のbreak削除・mono_&&legato_早期return削除、`FITOMBridge`のタイマースレッド化)のみコードに残っている | ✅ | — |
| ノートオン/オフのタイミング調査用デバッグログを追加(2026年7月新設、上記一連の「ノートオフ後もレジスタが更新され続ける」調査の一環。ユーザー実機での再現が上記3件の修正後もなお解消しないため、実測ログによる切り分けが必要になり追加)。`CInstCh::noteOn()`/`noteOff()`(MIDIメッセージ受信レベル、note/vel/mono_/legato_/解決したdev・devCh・isChOwnedBy結果を記録)と`CSoundDevice::noteOn()`/`noteOff()`(デバイスレベル、dev記述子・ch・note・wasReleasing・直前ステータスを記録)、`CSoundDevice::timerCallback()`のReleasing→Empty遷移(releaseTimer満了でのs.free()、該当chが実際にここへ到達しているか=設計通り一定時間で止まっているかを確認できる)にそれぞれ`FITOM_LOG_DEBUG`を追加した。boost::log既定のログ行にはマイクロ秒精度のタイムスタンプが自動付与されるため、追加のタイムスタンプ実装は不要だった。既定のログレベルは`info`(fitom_gui)/`debug`(fitom_cli)のため、fitom_gui側で確認するには`fitom.conf.json`(実行ファイルと同じディレクトリ、無ければ新規作成)に`{"log":{"level":"debug","file":"fitom_debug.log"}}`を設定する必要がある(`config_schema/fitom.conf.schema.json`参照) | ✅ | — |
| `wasReleasing`(リリース中再トリガー対策の発動フラグ)が通常のポリフォニック経路では常にfalseになり、実質一度も機能していなかったバグを修正(2026年7月、上記forceSteal修正後もユーザー実機での「高速なベースラインでアタックが再トリガーされず音程だけ滑らかに変わって聞こえる」症状が解消せず、上記デバッグログで実測した結果判明した真因)。実測ログでは、前の音のnoteOff(Running→Releasing)からわずか数ms後に同一ch(同一owner+patchによる通常のround-robin再利用)へ次のnoteOnが来ており、`wasReleasing=false`と出力されていた。`ChState::assign()`(`CSoundDevice::assignCh()`内で呼ばれる)がstatusをReleasing→Assignedへ無条件に書き換えてから、後で`ChState::run()`(`CSoundDevice::noteOn()`内で呼ばれる)が`status==Status::Releasing`を見て`wasReleasing`を判定していたため、assignCh()を必ず経由する通常のポリフォニック経路では、run()が判定する時点でstatusは既にAssignedに書き換わっており、wasReleasingは原理的に常にfalseにしかならなかった(モノフォニックの自己スティール経路[assignCh()を経由せずdevChを直接使い回す]でだけ偶然正しく動いていた)。「リリース中再トリガー対策」自体はOPM/OPN/OPL/OPL3全チップドライバに実装済み(`updateKey()`内の`if (keyOn && wasReleasing)`分岐、キャリアOPのRRを一時的に最大化してリリース中のエンベロープを急速ダンプしてからKey Onする)でコードは正しかったが、発動条件が常にfalseのため通常のポリフォニック演奏では一度も機能していなかった。`ChState::assign()`側でstatus書き換え前に`wasReleasing`を確定させ、`run()`は`status==Assigned`(=assign()経由)の場合はその値を信頼して上書きせず、assign()を経由しない経路(モノの自己スティール等)では従来通りrun()側で直接判定するよう修正した。`tests/test_soundevice_freq.cpp`に、Releasing中の同一chへ即座に再ノートオンした場合、`wasReleasing`がtrueになりキャリアOPのRR強制ダンプがKey Onより前に書かれることを検証する回帰テストを追加(修正前は`wasReleasing`が常にfalseのためRR強制ダンプの書き込み自体が無く失敗することを確認した上で、修正適用後にPASSすることも確認済み) | ✅ | `midi-implementation-status.md` |
| CC#1(Modulation)ソフトウェアLFOが、それを一度も送っていない別のMIDIチャンネルの発音に誤って掛かることがある不具合を修正(2026年7月、「CC#1でソフトウェアピッチLFOを使用した後、まったく別のCHで発音した音にLFOがかかることがある」との報告で発覚)。原因はデバイスチャンネル(物理ボイススロット)の使い回し。`CInstCh::setModulation()`は発音中の`notes_[]`(自チャンネルが現在所有するdevChのみ)にしか`ISoundDevice::setCC1Modulation()`をpushせず、`CInstCh::noteOn()`側も新規ノート確保時にCC#1値を対象devChへ再送していなかった(該当箇所に「CC#1(pmDepth_)はここでは一切参照しない」という明示コメントあり)。一方`VoiceProcessor::reset()`(`ChState::free()`/`init()`から呼ばれる)は`chLfo_`はリセットするが`cc1Value_`/`cc1LfoMode_`はクリアしない設計のため、あるMIDIチャンネルでCC#1>0を送った後そのdevChがボイススチール等で別のMIDIチャンネルへ再割り当てされると、`VoiceProcessor::onNoteOn()`が残留した`cc1Value_>0`を見て(新ノートの音色が`sw.LFR=0`の場合)ソフトLFOを誤って起動していた。同種の問題は既にCC#77(ソフトウェアLFO Depth上書き)で認識・対策済みで、`CInstCh::noteOn()`が`ISoundDevice::setLfoDepthOverride()`を毎ノートオン必ずpushする実装になっていたため、これと同じパターンで`dev->setCC1Modulation(devCh, pmDepth_, modDepthRange_)`を毎ノートオンで必ずpushするよう追加して解消した | ✅ | `midi-implementation-status.md` |
| プロファイルの`banks`セクションを外部ファイルへ分離可能に(2026年7月新設)。パッチバンク構成(bank/prog番号の割り当て)はデバイス構成(`devices`/`hw_plugins`)に一切依存しないため、`banks`に文字列を指定すると外部JSONファイルへのパスとみなし、参照先オブジェクト(`hw_banks`/`sw_banks`/`patch_banks`/`drum_banks`/`scc_wave_banks`/`pcm_banks`/`sf2_banks`を持つオブジェクト、内容は従来の`banks`オブジェクト直書きと同一スキーマ)をそのままその位置に埋め込んだものとして読み込む(パス解決はプロファイル自身のディレクトリが基点、既存の`banks.*[].file`と同じ規則)。これにより、デバイス構成が異なる複数プロファイル間で同じプリセットバンク構成ファイルを使い回せる(デバイス構成に含まれないバンクエントリは単に発音しないだけであり、デバイス構成の一部変更がbank/prog番号の一貫性を崩すことはない)。`FITOMConfig::buildFromProfile()`が`resolveBanksSection()`(新設の静的ヘルパー、`Config.cpp`)で`banks`の実体(オブジェクトそのもの、または文字列なら参照先ファイルをパースした結果)を1回だけ解決し、`loadDrumBanks()`/`loadSf2Banks()`双方へ渡すよう変更(両関数はプロファイル全体ではなく解決済みの`banks`実体を直接受け取るようシグネチャ変更)。`profileJson_`(ロード時の生JSON、`saveProfile()`の書き戻しベース)は無加工のまま保持されるため、外部参照形式で書かれたプロファイルは書き戻し時も文字列参照のまま維持される(展開後のオブジェクトで上書きされない)。`config_schema/profile.schema.json`の`banks`プロパティを`oneOf`(文字列/オブジェクト)化、`banks/README.md`に使用例を追記 | ✅ | — |
| `banks`と同じレベルに`bank_overrides`(同一スキーマ、オブジェクト直書きまたは外部参照ファイルパスの文字列)を新設(2026年7月)。外部参照の共通プリセットバンクセット(`banks`)に対し、プロファイルごとに一部のバンクだけを差し替えたり、共通セットに無いバンクを追加したりする用途。各配列種別の「識別キー」(`hw_banks`は`group`+`bank`、`drum_banks`は`bank`を持たないため`prog`、`pcm_banks`は同一bank番号を複数の物理チップ向けに使い分けられる設計のため`bank`+`chip`、それ以外[`sf2_banks`/`sw_banks`/`patch_banks`/`scc_wave_banks`]は`bank`)が一致するエントリは`bank_overrides`側の内容で置き換え、一致しなければ配列末尾へ追加する(削除はできない)。`FITOMConfig::buildFromProfile()`が`banks`解決直後に`bank_overrides`を`resolveExternalBanksRef()`(旧`resolveBanksSection()`をキー名引数化して汎用化)で解決し、`applyBankOverrides()`(新設、`mergeBankArray()`/`bankEntryKeyMatches()`)で`resolvedBanks`へマージしてから`loadSf2Banks()`/`loadDrumBanks()`へ渡す。`config_schema/profile.schema.json`は`banks`のオブジェクト形式スキーマを`definitions.banksObject`へ切り出し、`banks`/`bank_overrides`双方から`$ref`で共有するようリファクタリング(スキーマの二重管理を回避)。`banks/README.md`に識別キーの対応表と使用例を追記 | ✅ | — |
| OPLL/PSG(拡張5bit音量版)の音量変換で、中音量域を跨ぐと突然無音化し、かつ最大音量に到達しないバグを修正(2026年7月、ユーザーとの設計レビューで`fitom::linear2dB()`のOPL向け実装の妥当性を確認していた際、隣接するOPLL実装の挙動が数値検証で食い違うことに気付き発覚)。`linear2dB(evol, range, step, bw)`は`evol`(0-127のラウドネス値)を`kGM2dB[128]`テーブル(0.75dBステップ、96dBレンジ)で引いてからチップのレジスタ幅(`bw`)へ最終シフト(`>>= 7-range-bw`)で丸め込む設計で、最終的な1レジスタ刻みあたりのdB幅はこのシフト量だけで決まる(OPLLの場合4bit/RANGE48DBで2bitシフト=0.75dB×4=3dB、実機YM2413の3dB/16段仕様と一致)。ところが`evol`側の前処理`evol = evol & step`に、OPL(`STEP075DB=0x7F`、実質無マスク)以外のOPLL(`STEP150DB=0x3F`)・PSG拡張5bit版([PSG_new.cpp](../core/src/PSG_new.cpp)、同じく`STEP150DB`)が、evolの上位bit(値64)を無条件に切り捨てるマスクを使っていたため、evol=64以降がevol=0-63へ折り返され(周期64のエイリアシング)、音量がloudness=63→64の境界で無音へ飛び、loudness=127まで上げても実機レジスタの最大音量(0)へ二度と到達しないという不具合になっていた。当初「1bit右シフトしてからマスクする意図では」という代替案も検証したが、シミュレーションの結果、不連続は解消するものの`kGM2dB`テーブルの大音量側半分(index64-127、-12dB〜0dB)を参照できなくなり最大音量に到達しない問題は残ることを確認し、採用しなかった。最終的にOPL同様`STEP075DB`(無マスク)へ統一し、evolを0-127のフルレンジのまま`kGM2dB`へ渡すよう修正(`OPLL_new.cpp`のトーン/リズム双方のパート、`PSG_new.cpp`の拡張5bit音量版)。`docs/manuals/hwpatch-reference.md`のOPLL系`ops[1].TL`の説明(誤って「1.5dBステップ」としていた記述)も実際のステップ幅(3dB)に訂正 | ✅ | `hwpatch-reference.md` |
| CC#1(Modulation)/CC#77(Soft LFO Depth上書き)の「毎ノートオンで前の占有者の残留値を強制上書きする」対策が、実質的に一度も機能していなかった不具合を修正(2026年7月、上記CC#1修正の効果検証中に「同じ手順を踏んでも別MIDIチャンネルへのLFO漏れが再現する」と報告され発覚。当初は上記修正の再発と見えたが、実際には最初から有効になったことがなかった)。`CInstCh::noteOn()`が`ISoundDevice::setCC1Modulation()`/`setLfoDepthOverride()`を呼ぶのは、`dev->assignCh()`(`ChState::status`を`Assigned`にする)の直後・`dev->noteOn()`(`status`を`Running`にする)より前のタイミングだが、`CSoundDevice::setCC1Modulation()`/`setLfoDepthOverride()`側のガードが`ChState::isActive()`(`Running`/`Releasing`のみtrue、`Assigned`はfalse)だったため、devChを他のMIDIチャンネルから奪う通常の経路(`assignCh()`を必ず経由する)では、この2関数の呼び出しが常に`status==Assigned`の状態で早期returnし、目的の強制pushが一切実行されていなかった。同一MIDIチャンネル内でのモノ/レガート自己スティール(`assignCh()`を経由せず`status`が`Running`/`Releasing`のまま)の場合のみ偶然有効だったため、この経路でしか検証していなければ機能しているように見えてしまう。`CSoundDevice::setCC1Modulation()`/`setLfoDepthOverride()`のガードへ`isAssigned()`を追加し、`Assigned`状態でも forced push が効くよう修正した(`isActive()`自体の定義は他の呼び出し箇所[`isChOwnedBy()`・`noteOff()`・ボイススティール年齢判定等]に影響するため変更していない) | ✅ | `midi-implementation-status.md` |
| パッチピッカーでAWMバンクが常に空欄で表示される不具合を修正(2026年7月、`unified.bankset.json`にAWMバンク[YRW801 GM/Drum]が2件登録されているにもかかわらず、GUIのパッチピッカー[CC#0=AWM]でバンク一覧が空になると報告され発覚)。真因は`SampleZoneBankRegistry`(`PatchData.h`)がバンク番号のみをキーにしたフラットな名前空間だったこと。同プロファイルは`hw_banks`でAWMバンク0/1(`opl4awm_yrw801_gm/drum.samplezonebank.json`)を読み込んだ**後**、`pcm_banks`でADPCM-B/ADPCM-Aのバンク0/1(`PatchManager::loadPcmBankJson()`のnamed patch自動合成先も同じ`sampleReg_`)を読み込んでおり、`FITOMConfig::voicePatchTypeToVoiceGroup()`がADPCM-B/ADPCM-A/PCMD8/AWMを全て`VOICE_GROUP_PCM`へ束ねる設計上、この4チップ族はそもそも同じバンク番号空間を共有できないにもかかわらず、`SampleZoneBankRegistry`側にはその区別が無く、後から読み込まれたADPCM-B/ADPCM-Aのバンク0/1が同じキー(バンク番号)でAWMバンク0/1を上書きしていた(実機ログで`SampleZoneBank loaded ... voicePatchType=0x54`の直後に`PCM Bank: auto-synthesized SampleZoneBank ... voicePatchType=0x51/0x52`が同じバンク0/1に対して出力されることを確認し特定。`FITOMBridge::getHwBankList()`にデバッグログを一時追加した実機再現テストで、`pm.sampleRegistry().listBankNumbers(0x54)`が上書き後に0件を返すことも確認済み)。`SampleZoneBankRegistry`を`HwBankRegistry`と同じ二重mapキー方式(`unordered_map<uint8_t, unordered_map<int, SampleZoneBank>>`)に変更し、バンク番号だけでなくvoicePatchType自体をキーに含めるよう修正(**VoiceGroupではなくvoicePatchType直接をキーにする必要がある点に注意**。VoiceGroupだと上記の通りADPCM-B/ADPCM-A/PCMD8/AWMが再び同じ名前空間に束ねられてしまうため)。`getOrCreate()`/`find()`/`hasBank()`/`resolve()`の全シグネチャに`voicePatchType`引数を追加し、呼び出し元(`PatchManager::resolveDirect()`/`loadSampleZoneBankJson()`/`saveSampleZoneBankJson()`/`loadPcmBankJson()`、`FITOMBridge`の`getChannelMonitors()`/`getHwBankList()`/`getHwBankPatches()`)を追従させた。`tests/test_config.cpp`に、AWM(`hw_banks`)とADPCM-A(`pcm_banks`)が同一バンク番号0を使っても互いに上書きしないことを検証する回帰テストを追加 | ✅ | `patch-structure-design.md` |
| OPLビルトインリズムが一切発音しない不具合を修正(2026年7月、ユーザーが`emu_opl.profile.json`でドラムキット[prog13、`opl_builtin_rhythm.drumkit.json`]を選択しても、レジスタダンプモニター・レベルメーターともに無反応と報告され発覚)。`COPLRhythm`(`DEVICE_OPL_RHY`)本体の実装・`VOICE_PATCH_OPL_RHY`によるパッチ解決経路(`PatchManager::resolveDirect()`のext.rhythmCh解決)・対応するHwBank(`msx_audio_preset_rhythm.hwbank.json`)/DrumKitデータは先行するセッションで整備済みだったが、`FITOMConfig::resolveCompositeSpec()`にOPL/OPL2/OPL3/OPL4向けの`DEVICE_OPL_RHY`生成分岐が一つも無く、どのOPL系プロファイルでも内蔵リズムチャンネルのデバイス自体が実行時に存在しないままだった(`findDeviceIndexByVoicePatchType(VOICE_PATCH_OPL_RHY)`が常に-1を返し、`resolveDirect()`が「no matching device」で無音のまま握りつぶしていた)。既に確立済みのOPLL系パターン(FM本体+`_RHY`サブデバイスが同一物理ポートを共有し、`rhythm_mode`プロファイル設定でFM本体側のch6-8を無効化する)をOPL系にもそのまま適用(`COPL`/`COPL2`/`COPL3_2`に`rhythmMode`コンストラクタ引数を追加、`resolveCompositeSpec()`にOPL/Y8950/OPL2/OPL3/OPL4の各caseを新設)。OPL3/OPL4はリズムモードレジスタ(0xBD)がbank0限定のハードウェア制約のため、`COPL3_2`のport1サブチップ(chip1_)側のch6-8のみを無効化し、port2側3chは通常の2OPとして使い続けられる設計とした。`tests/test_config.cpp`に、OPL/Y8950/OPL2/OPL3/OPL4それぞれの`resolveCompositeSpec()`が`DEVICE_OPL_RHY`サブデバイスを含むことを検証する回帰テストを追加(既存のDEVICE_OPL4テストも3→4エントリへ更新)。**既知の制限(下記「OPL/OPLL系ビルトインリズムのプロファイル明示化」で挙動修正済み)**: `auto_devices`(`HWPlugin_Enumerate()`をそのまま`devices[]`として使う経路)では個々のチップに`rhythm_mode:true`を指定する手段が無いため、内蔵リズムを有効化できない(ビルトインリズムを使うには`devices[]`を手書きして`rhythm_mode:true`を明示する必要がある) | ✅ | `chip-driver-architecture.md`, `config-design.md` |
| OPL/OPLL系ビルトインリズムのプロファイル明示化(2026年7月、上記OPLビルトインリズム修正の直後にユーザー指摘で発覚)。上記修正は`DEVICE_OPL_RHY`を生成する分岐自体を新設したが、`resolveCompositeSpec()`側でその生成を`rhythm_mode`の値で分岐しておらず、OPL/OPL2/OPL3/OPL4・OPLL系のいずれも該当チップ種別が`devices[]`に1つでもあれば`rhythm_mode`指定の有無に関わらず常に`_RHY`サブデバイスが生成されていた(「既知の制限」に記載の衝突が、実際にはrhythm_mode:falseの既定プロファイルでも常に起きる状態だった)。ビルトインリズムと通常の楽音chをまたいだ動的ボイス割当(DVA)は実装されていないため、`resolveCompositeSpec()`の引数にそのデバイスインスタンス自身の`rhythm_mode`値を追加し、OPL系/OPLL系は`rhythm_mode:true`の場合のみ`DEVICE_OPL_RHY`/`DEVICE_OPLL_RHY`をoutSpecへ追加するよう修正(FM本体側ch6-8の無効化は既存通り同じフラグで行われる)。これによりチップインスタンスごとにビルトインリズムの使用可否を明示的に選択でき、同一チップ種別を複数`devices[]`エントリで持つ場合でも`rhythm_mode:true`が1つも無ければリズムサブデバイス自体が一切生成されない。OPNA系(`DEVICE_OPNA_RHY`)はFM本体と完全に独立したレジスタ空間を持ち共有・DVA調整が不要なため対象外とし、従来通り常時生成する。`tests/test_config.cpp`のOPL/OPL2/OPL3/OPL4回帰テストを`rhythm_mode:true/false`両方で検証するよう更新し、OPLL用のテストも新設 | ✅ | `chip-driver-architecture.md`, `config-design.md` |
| リズムチャンネルのBank/Program表示をダブルクリックした際、外部パッチエディタをドラムキット編集モード(`kind="drum"`)で起動できるよう対応(2026年7月、ユーザーが「OPLビルトインリズム」チャンネルでダブルクリックすると「このチャンネルには現在編集可能なパッチがありません」エラーになると報告し発覚)。従来`FITOMBridge::resolveChannelHwPatch()`は`midich->isRhythm()`で即座にfalseを返す設計だった(「ノート単位で複数のHwPatchを使いうるため単一の『現在のパッチ』が無い」という理由)が、FITOM_patch_editor側は既にD-040で`kind="drum"`(*.drumkit.json単位、「1ファイル=1キット」なのでbank-fileだけでキットが一意に決まり、prog引数は`DrumKit::prog`との整合性チェックにのみ使われる)をキオスク対応済みだった。`resolveChannelHwPatch()`をリズムチャンネル専用の解決パス(`pm.drumRegistry().resolve(getBankNo(), getProgramNo())`→`DrumPatch::filename`)に変更。`DrumPatch`(`DrumData.h`)に読み込み元ファイルパスを保持する`filename`フィールドを新設し、`PatchManager::loadDrumKitJson()`で設定するよう修正(既存の`DrumPatchBank::filename`はバンク単位[複数progが共有・最後に読んだファイルで上書きされる]のため、prog単位の対象ファイル特定には使えなかった)。`tests/test_config.cpp`に、複数progを読み込んでもそれぞれが自分自身の`DrumPatch::filename`を保持することを検証する回帰テストを追加(`FITOMBridge`はfitom_core単体のテストにリンクされないため、コア層の`PatchManager::loadDrumKitJson()`を直接検証)。**未実施**: 実際にFITOM_patch_editorが`kind="drum"`で正しいドラムキット編集画面を開くことの実機確認(上記の`kind="device"`/`"layered"`と同様、4引数キオスク起動の実機動作確認自体が未実施のまま) | ✅ | — |
| `hw_plugins[].auto_devices`経由のプロファイルでビルトインリズムを有効化する手段が無かった制約を解消(2026年7月、ユーザーが`emu_opl.profile.json`[FitomEmuIF、`auto_devices:true`]でOPLビルトインリズムのノートを鳴らしても`resolveDirect: OPL_RHY: ... ext.rhythmCh=4 is unset or out of range (chCount=0)`という警告のみで無音のままと報告し発覚)。直前の「OPL/OPLL系ビルトインリズムのプロファイル明示化」修正で`DEVICE_*_RHY`サブデバイスの生成を`rhythm_mode:true`のインスタンス限定にした結果、`auto_devices`(`HWPlugin_Enumerate()`が返すJSON配列をそのまま`devices[]`として使う経路)ではそもそも列挙結果に`rhythm_mode`という概念自体が含まれない(HWプラグイン側はFITOM_X固有のデバイス合成概念を関知しない設計のため)ため、`rhythm_mode:true`を指定する手段が無く、OPL系内蔵リズムが`auto_devices`環境では原理的に有効化不可能になっていた(chCount=0は`findDeviceIndexByVoicePatchType(VOICE_PATCH_OPL_RHY)`が-1を返し`DEVICE_OPL_RHY`サブデバイス自体が存在しないことの帰結)。`hw_plugins[]`エントリに新設した`auto_devices_rhythm_mode`(チップ名の配列、`config_schema/profile.schema.json`)で、列挙結果の`"chip"`名がこの配列に含まれるエントリにだけ、`buildDevice()`へ渡す直前に`rhythm_mode:true`を補うよう`FITOMConfig::buildFromProfile()`のauto_devicesループを拡張した。`devices[]`を手書きする場合(実機用プラグイン等)は従来通り各エントリへ直接`rhythm_mode`を書けばよく、この新設フィールドは`auto_devices:true`使用時のみ意味を持つ。auto_devicesは実プラグインDLLの`HWPlugin_Enumerate()`呼び出しに依存し既存のユニットテスト基盤にモックが無いため、自動テストは追加していない(コードレビューと実機確認で検証) | ✅ | `config-design.md` |
| `PatchManager::resolveDirect()`(OPL_RHY)・`resolveBuiltinRhythm()`(0x70、OPNA/OPLL)双方に共通する、`chCount`取得ロジックの潜在バグを修正(2026年7月、上記`auto_devices_rhythm_mode`修正・`devices[]`手書き化の両方を適用しても、ユーザーの実機で`resolveDirect: OPL_RHY: bank=0 prog=0 ext.rhythmCh=4 is unset or out of range (chCount=0)`が変わらず出続けると報告され発覚。デバイス初期化ログには`Device[1]: Y8950-RHYTHM → OPL Rhythm 5ch`が正しく出ており、`DEVICE_OPL_RHY`サブデバイス自体は生成されていた)。真因は`config.getDevice(deviceIndex)`(`FITOMConfig::getDevice()`)が**常にnullptrを返す**ことだった: `FITOMConfig`は`IPort`を所有するのみで、実際に稼働する`ISoundDevice`インスタンスは`CFITOM::devices_`が別途所有する設計(`CFITOM.h`の「ConfigはIPortを所有し、CFITOMはISoundDeviceを所有する」というコメント通り)であり、`FITOMConfig::DeviceEntry::device`(`unique_ptr<ISoundDevice>`)フィールドは一度も書き込まれない未使用コードだった。この落とし穴自体は`gui/bridge/FITOMBridge.cpp`に「`fitomInst.getDevice()`を使うこと(`cfg.getDevice()`は常にnullptrを返す)」という既存コメントで既に文書化されていたが、`PatchManager.cpp`側の2箇所(`resolveDirect()`のOPL_RHY分岐、`resolveBuiltinRhythm()`)はこの罠を踏んでおり、`chCount`が常に0になるため、リズムパートのプログラム番号(hwProg/ext.rhythmCh)が何であっても必ず範囲外判定になり無音のまま握りつぶされていた。**このバグはOPL_RHYに限らず、OPNA/OPLLの内蔵リズム(VOICE_PATCH_BUILTIN_RHYTHM=0x70経路)にも同一パターンで存在しており、機能として使われた形跡がなかった可能性が高い**(このコードパス自体、直前まで「一致するデバイスが無い」でより手前で早期returnしていたため、後段の`chCount`チェックへ到達すること自体がこれまで一度も無かったと考えられる)。`chCount`はchipType固有の固定値であり稼働中のインスタンスを必要としないため、`config.getDevice(idx)->getChCount()`ではなく`DeviceFactory::defaultChCount(deviceType)`(静的テーブル、`DeviceFactory.cpp`)を参照するよう両箇所を修正した。統合テスト(`resolveDirect()`のOPL_RHY分岐)は実HWプラグインDLLの読み込みに依存し既存のテスト基盤にモックが無いため自動テストは追加していない(コードレビュー・実機確認で検証)。**関連する既知の問題(未修正のまま)**: `gui/bridge/FITOMBridge.cpp`のデバイス一覧取得コード(`cfg.getDevice(i)`を使う箇所、`getHwBankList()`近辺)にも同じ「常にnullptr」の影響が残っているが、対応するGUI画面(デバイス一覧)自体が`STATUS.md`記載の通り未実装(導線が無い)ため実害は無い。将来デバイス一覧画面を実装する際は`cfg.getDevice()`ではなく`fitomInst.getDevice()`を使うこと | ✅ | — |
| OPLビルトインリズムでバスドラム(BD)だけ発音せず、他4楽器(HH/SD/TOM/CYM)は正常に鳴る不具合を修正(2026年7月、上記chCount修正でOPLビルトインリズムが実機で発音するようになった直後、別セッションからの報告[静的解析でBD限定の実装差を特定済み]を受けて着手)。`COPLRhythm::writeOperatorRegs()`(`OPL_new.cpp`)が、キャリアオペレータのエンベロープ(AR/DR/SL/SR/RR)を`VoiceProcessor`から読む際、引数で渡された`opIdx`(物理レジスタスロット番号)をそのまま`proc.velAR(0)`のように`0`決め打ちで流用していた。HH/SD/TOM/CYMは単一オペレータ(`hwOp[0]`のみ使用、`VoiceProcessor`側もop0基準で計算)のため偶然正しく動作する一方、BDだけ2オペレータ構成でキャリアが`hwOp[1]`(op1)なのに、そのエンベロープへ誤ってモジュレータ側(op0)の`VoiceProcessor`補正値を適用してしまっていた(音量(TL)側は`updateVolExp()`が`effectiveTL(i)`と正しくインデックスしていたため無事だった、というBD限定の症状と整合)。`writeOperatorRegs()`に、物理レジスタスロット番号(`opIdx`、レジスタアドレス計算専用)とは独立した`velOpIdx`(HwPatch自身のオペレータ番号、`VoiceProcessor::onNoteOn()`の補正値配列への添字専用)引数を新設し、呼び出し元(`updateVoice()`)がBDでは実際のオペレータ番号(`i`)、単一オペレータ楽器では常に`0`を渡すよう分離した。`tests/test_soundevice_freq.cpp`に、BDのop0/op1へ大きく異なるAR値を設定し、実際にレジスタへ書き込まれる値がop1(キャリア)基準になっていることを検証する回帰テストを追加(修正前のコードに対して実行し、期待通り失敗する[op0の値が書き込まれる]ことも確認済み)。`docs/patch-structure-design.md`の「`updateVoice()`/`onNoteOn()`呼び出し順序の根本的欠陥」節に追記(同じ`writeOperatorRegs()`が関わるが原因は別の不具合であることを明記) | ✅ | `patch-structure-design.md` |
| Y8950(YM3801、MSX-AUDIO)をデバイス登録しても内蔵ADPCM-Bインスタンスが一切生成されないバグを修正(2026年8月、ユーザーが「デバイスとしてY8950が登録されているのにY8950のADPCM-Bインスタンスが作成されていないように見える」と指摘し発覚)。`DEVICE_ADPCMB_Y8950`というdeviceType自体・`DeviceFactory`のルーティング(`createCAdPcm()`→`CYmDelta`)・`VoicePatchType`変換(`VOICE_PATCH_ADPCMB`)は先行するセッションで一通り実装済みだったが、`FITOMConfig::resolveCompositeSpec()`が`DEVICE_OPL`(YM3526、ADPCM非搭載)と`DEVICE_Y8950`を同一`case`にまとめてしまっており、FM本体+(rhythm_mode時のみ)`DEVICE_OPL_RHY`しか`outSpec`へ積んでいなかったため、プロファイルに`chip:"Y8950"`と書いてもADPCM-Bサブデバイスが一度も自動生成されない状態が残っていた(OPNAの`DEVICE_ADPCMB_OPNA`と同じ「配線待ち」パターン)。`DEVICE_OPL`と`DEVICE_Y8950`の`case`を分離し、`DEVICE_Y8950`のみ`DEVICE_ADPCMB_Y8950`を追加するよう修正。ADPCM-Bのレジスタ空間はFM本体のオペレータレジスタ・リズム用ch6-8とは独立しているため、OPNA系の`DEVICE_ADPCMB_OPNA`同様`rhythm_mode`の値に関わらず常に生成する(OPL_RHYのみ従来通り`rhythm_mode:true`時限定)。`tests/test_config.cpp`のY8950回帰テストを、rhythm_mode:trueでspec.size()==2→3(FM+ADPCMB+RHYTHM)、false でspec.size()==1→2(FM+ADPCMB)へ更新 | ✅ | `chip-driver-architecture.md` |
| MIDIノートとFnum空間のマッチング改善: 高音域でFnumがオーバーフローする不具合を修正(2026年8月、ユーザーが「高音域でFnumがオーバーフローしている」と指摘し着手)。`CSoundDevice::getFnumber()`(`SoundDevImpl.cpp`、`FnumTableType::Fnumber`/`TonePeriod`/`SSG`を使う全チップドライバの標準実装。OPN/OPL/OPLL/PSG系[SSG/DCSG/SCC/EPSG]が該当)のblock(オクターブ)は、コンストラクタで`fnumTable_`へ焼き込まれる`noteOffset_`とは独立に、MIDIノート番号(`lastNote`)のみから`floor(lastNote/12)`相当で決まる。MIDIノートは0-127(約10.6オクターブ)ある一方、blockの格納先は3bit(0-7の8オクターブ)しかないため、高音域(note>=96)でblockが8を超え、`oct>7`クランプ処理(blockを7に固定しfnumを左シフトで補う、SoundDevImpl.cpp)がfnumを11bit(2047)超へ押し上げてしまっていた(実際に検証すると、note=96でfnum=2473・note=127でfnum=14825まで増大することを確認)。基準ノートの標準オフセット`FNUM_OFFSET`(`Fnum.h`)を`-576`(-9半音)から`-1344`(-576-768、1オクターブ=768を追加で減算)へ変更し、基準ノートを1オクターブ下げることで、同じ絶対音高に対するblockを一律1つ減らし、オーバーフロー発生開始点をnote96→108へと1オクターブぶん高音側へ押し上げた(A4=note69の実効blockも6→5相当にシフトする)。`OPN_new.cpp`・`PSG_new.cpp`(`CDCSG`)にハードコードされていた`-576`リテラル、`ISoundDevice.h`の`CSoundDevice`コンストラクタのデフォルト引数`-576`も、単一の`FNUM_OFFSET`マクロ経由に統一(以前は同じ値がコード中に分散しており、チップファミリー間の一貫性を保つには手作業での同時変更が必要だった)。**対象外(意図的)**: `COPM::getFnumber()`(独自KeyCode方式、`noteOffset_=-61`で別スケーリング済み・実測でも「オーバーフロー」ではなく「範囲外で無音化」という別の症状であり、既存の検証コメント[note=69→KC=0x4C]付きで別途キャリブレーションされているため今回は対象外)、ADPCM系の`kNoteOffset`(448/356、DeltaNは16000Hz基準の実機再現用の別系統で、生成テーブル自体が0-65535へクランプ済みのためオーバーフローが起きない)。`tests/fitom_tests`は既存回帰テスト(note=60/72/90を使用、いずれもblock<=7の範囲でオフセット変更の影響を受けない)が全件PASSすることを確認済み | ✅ | — |
| OPL/OPLLビルトインリズムで、Fnumレジスタを共有するチャンネル(ch7=HH/SD共有、ch8=CYM/TOM共有)のうちHH・CYM側のFnum更新を無効化(ユーザー要望: HH・CYMはノイズ性の発振でFnumがほぼ発音に寄与しない一方、SD・TOMはピッチシフト演奏に意味があり、後着優先の上書き仕様だとHH・CYMの発音がSD・TOMのFnumを意図せず潰してしまうため)。`COPLRhythm::updateFreq()`(`OPL_new.cpp`)・`COPLLRhythm::updateFreq()`(`OPLL_new.cpp`)の両方に、論理ch0(HH)・ch1(CYM)なら即returnしFnumレジスタ(OPL: 0xA0+physCh/0xB0+physCh、OPLL: 0x10+vch/0x20+vch)へのアクセス自体を行わないガードを追加。KEYON制御は両チップともFnumレジスタとは独立した専用レジスタ(OPL: 0xBD、OPLL: 0x0E)で行われるため、この変更によるキーオン/オフへの影響は無い。`tests/test_soundevice_freq.cpp`に、SD(ch3)でFnumを書き込んだ後にHH(ch0、同じ物理チャンネルを共有)へ大きく異なるノートでノートオンしても、共有チャンネルのFnumレジスタがSDの値のまま変化しないことを検証する回帰テストをOPL/OPLL双方に追加(修正前のコードに対して実行し、期待通り失敗する[HHのFnumで上書きされる]ことも確認済み)。`docs/terminology.md`のCOPLLRhythm節を更新(OPL系のCOPLRhythmも同じ設計であることを明記) | ✅ | `terminology.md` |
| レジスタダンプモニターがSF2ラッパーデバイス(`chip:"SF2"`、`DEVICE_NONE`)を物理チップとして誤って列挙する不具合を修正(2026年8月、上記SF2直行パス実装をFitomSf2IF側の実装内容と突き合わせるレビュー中に発覚)。`CFITOM::buildPhysicalChipList()`が`deviceType`を見ずに`config_->getDeviceCount()`を素通しで走査していたため、実レジスタ空間を持たないSF2ラッパー(`initDevices()`は`ISoundDevice`生成自体をスキップする)が、生MIDIバイト列を書き込んだ`HWPort`のシャドウレジスタをそのまま「物理チップ」としてRegisterDumpWindowに表示してしまっていた(SF2固有ではなく、単なる未知の`chip`文字列を指定した場合にも本来起きていた潜在バグ)。`deviceType == DEVICE_NONE`のエントリをループ先頭でスキップするよう修正 | ✅ | — |
| SF2直行パスのプリセット名表示のための`phdr`パースを実装(2026年8月、`docs/sf2-fluidsynth-integration.md`⑧節で方針決定済みだった残作業に対応)。SoundFont2(RIFF/sfbk)ファイルは`LIST/pdta/phdr`サブチャンクにプリセット名(`achPresetName`)・バンク番号(`wBank`)・プログラム番号(`wPreset`)を持つ固定長(38byte)レコード配列を格納している。新設`fitom::parseSf2PresetHeaders()`(`Sf2BankRegistry.h/.cpp`)がこのサブチャンクのみを読み、音声サンプル本体(`LIST/sdta`、ファイルサイズの大半を占めうる)はチャンクサイズでシークして読み飛ばす(fluidsynthや外部ライブラリへの依存なし)。末尾の終端センチネルレコード(慣習的に"EOP"という名前を持つ、実プリセットではない)は結果に含めない。`Sf2BankRegistry`が新規`file`の初出時(`soundfont_index`採番と同じタイミング)に一度だけこれを呼び、`(sf2_bank, program) → プリセット名`のルックアップテーブルを構築、`resolvePresetName(cc32Bank, prog, outName)`(CC#32解決→ファイル/sf2_bank特定→名前引きの2段変換)で解決できるようにした。ファイルが開けない・RIFF/sfbk形式でない・phdrチャンクが無い等、パースできない場合は例外を投げず空配列を返す(呼び出し元は既存の直接デバイス選択モード等と同様、数値フォールバックする設計を維持できる)。`tests/test_config.cpp`に、手組みの最小限SoundFont2バイナリ(実プリセット2件+終端センチネル、INFO/sdtaは空リストでスキップ経路も確認)を使った`parseSf2PresetHeaders()`単体テスト、`Sf2BankRegistry::resolvePresetName()`のテスト、`FITOMConfig::loadProfile()`経由のend-to-endテストを追加。**未着手**: `resolvePresetName()`をMIDIモニターのBank/Program列表示へ実際に配線する作業(Device/Fnumber列の表示方式が別途未確定のため、`sf2_channel_windows`編集用GUIダイアログとあわせて別途対応) | ✅ | `sf2-fluidsynth-integration.md` |
| 演奏中に`fitom_gui`が`0xC0000005`(アクセス違反)でクラッシュする不具合を修正(2026年8月、ユーザーがイベントログとクラッシュダンプ[`%LOCALAPPDATA%\CrashDumps`に自動保存されたもの]の調査を依頼し、Releaseビルドにシンボルが無かったためデバッグビルドで再現・Visual Studioのローカル変数ウィンドウで`ls.dev`(`CRhythmCh::LayerSlot::dev`)が`nullptr`であることを直接確認して特定)。クラッシュ箇所は`CRhythmCh::timerCallback()`(`MidiCh.cpp`)内の`ls.dev->setNoteFine(...)`で、直前の`if (!ls.isActive())`(`dev != nullptr`判定)を通過した直後にも関わらず`dev`が`nullptr`になっていたことから、ループ本体の実行中に別スレッドが同じ`LayerSlot`を横からクリアしたことが確定した。`timerCallback()`は`CFITOM`専用の`timerThread_`(`CFITOM::timerThreadFunc()`)から呼ばれ、`CFITOM::timerCallback()`が`processMutex_`をロックしてから`processors_[]`/`channels_[]`へディスパッチしている。GUIからのノート試聴(`CFITOM::sendChannelNoteOn/Off()`等)も同じ`processMutex_`を正しく取っているが、**実際の外部MIDI入力(MIDIバックエンドDLLのコールバックスレッドから直接呼ばれる`MidiProcessor::receiveByte()`、内部用MIDIパイプ[`backends/midi_pipe`]経由のパッチエディタ接続を含む)だけが`processMutex_`を一切取っていなかった**ため、MIDI入力スレッドが受信したNote Offを処理して`NoteSlots::stopAll()`で`LayerSlot`をクリアするのと、タイマースレッドが同じ`noteSlots_[note]`を反復処理するのが無同期で競合し、`isActive()`チェック直後という僅かなタイミング窓で割り込まれるとクラッシュしていた(GUI単体の操作では再現せず、実MIDI入力がある「演奏中」にしか起きない不具合だった理由と整合)。`CFITOM`に`receiveMpuByte()`/`receiveInternalPipeByte()`を新設し、`sendChannelControlChange()`等と同じ`processMutex_`ロック方針に統一した上で、`MidiProcessor::receiveByte()`への直接呼び出しをこれら経由に置き換えた(`gui/bridge/FITOMBridge.cpp`のMIDI入力ポート・内部パイプ双方のコールバック、`apps/fitom_cli/main.cpp`の対応箇所)。スレッド競合の性質上、既存のテスト基盤で決定的に再現するユニットテストは追加していない(デバッグビルドでの実機再現・コードレビューで検証) | ✅ | — |
| OPL/OPLLビルトインリズムのrhythm_mode有効時、元チップのch6-8(HH/CYM/TOM/SD/BDに転用済み)が、`CSpanDevice`による同種デバイス自動束ね(`mergeSpannableDevices()`)が絡む場合にDVA対象外・レベルメーター無効表示のいずれにもならないことがある不具合を修正(2026年8月、ユーザーが「rhythm_mode有効時、元チップのCH7-9はDVA対象外のはずなのでレベルメーター上も無効になる方がよい」と要望。調査の結果、レベルメーター表示機構自体(`ChState::isEnabled()`→`FITOMLevelChannel::enabled`)は既に正しく実装済み[2026年7月]だったが、束ねが絡むケースでその手前のch無効化自体が効いていない実際のバグが見つかった)。`Config::mergeSpannableDevices()`が束ねる各デバイスの`deviceType`は`PortGroup`(`Config.h`)に個別保持していたが`rhythm_mode`は保持しておらず、`CFITOM::initDevices()`のサブチップ生成ループが全サブチップに代表デバイス1つぶんの`rhythm_mode`(`config_->getDeviceRhythmMode(i)`)を一律適用していた。そのため、同一`VoicePatchType`の`rhythm_mode:true`なチップと`rhythm_mode:false`なチップが(グループ化キーが`VoicePatchType`のみのため)束ねられると、どちらが代表になるかによって「束ねられた側のch6-8無効化が効かない」(DVA・レベルメーター双方でリズムパートとレジスタを共有する楽音chが生きたまま残る、レジスタ衝突の実害あり)、または「無効化不要な側にまで誤って波及する」(該当チップの3chが常時レベルメーターで無効表示になる)のいずれかが起きていた。`PortGroup`に`rhythmMode`フィールドを追加して`mergeSpannableDevices()`が各サブチップの値を個別に保持するよう修正し、`FITOMConfig::getDeviceSpanGroupRhythmMode(index,k)`(`getDeviceSpanGroupDeviceType()`と対になる新設アクセサ)経由で`CFITOM::initDevices()`のサブチップ生成ループがこちらを参照するよう変更。実際のデバイス構築(`buildDevice()`)は実HWプラグインDLLの読み込みに依存し既存のテスト基盤にモックが無いため自動テストは追加していない(コードレビューで検証) | ✅ | `chip-driver-architecture.md` |
| RPN#1(チャンネルファインチューニング)で指定した値と実際のピッチが食い違う不具合を修正(2026年8月、ユーザーが「設定値と実際のチューニングが大きくずれる」と報告し着手)。独立した4つの不具合が重なっていた。**(1) Data Entry LSB(CC#38)が一切反映されていなかった**: `MidiProcessor::processControl()`(`CFITOM.cpp`)がRPN/NRPNへの適用をCC#6(Data Entry MSB)受信時にしか行わず、CC#38は`rpn_[ch].lsb`へ保存するだけだった。MIDIの通常の送信順は「CC#6 → CC#38」のため、LSBは常に1メッセージぶん遅れて**次の**CC#6適用時に紛れ込む状態で、しかもパラメータ番号を選び直しても`lsb`がクリアされないため別パラメータへ書いた残骸まで混入していた。14bit値全体を使うRPN#1では、これがそのままピッチのずれとして出る。`RpnState`にMSBと「現在のパラメータ選択後にCC#6を受け取ったか」のフラグを追加し、CC#6/CC#38の両方で14bit値を合成して適用する`applyDataEntry()`へ集約。CC#98/99/100/101でのパラメータ選択時はMSB/LSBを両方クリアし(`selectRpnParam()`)、MSB未受信のCC#38単独では適用しない(MSBを0とみなすと中央値0x2000基準のRPN#1でピッチが1半音近く飛ぶため)。**(2) `applyPitchBendToAll()`がトランスポーズを無視していた**: `setNoteFine()`へ渡すノート番号に`NoteHist::note`(受信したMIDIノート、本来はNoteOffの一致判定用)を使っていたため、`ToneLayer.transpose`や`SwPatch.fineTranspose`を持つ音色は、ピッチベンド・RPN・マスターチューニング等の再計算が走った瞬間にトランスポーズが丸ごと消えて音程が飛んでいた。`NoteHist`に「実際にデバイスへ渡したノート」(`devNote`)を追加して常にこちらを使うよう修正。**(3) ピッチオフセットの計算式が3経路で食い違っていた**: `noteOn()`はRPN#1をMSBのみで扱って下位7bitを捨て、さらにRPN#2コースチューニングとScale/Octave Tuningを一切加算していなかった。`applyPitchBendToAll()`は逆に`SwPatch.fineTranspose`の半音未満の端数(最大99セント)を取りこぼし、ポルタメント更新(`timerCallback()`)にいたっては全てのチューニング成分を無視してグライド分だけを書いていた。結果として「発音直後」「その後CC/ベンドを受けた後」「ポルタメント中」でピッチが変わっていた。共通部分(ピッチベンド+RPN#1+RPN#2)を`CInstCh::commonFineKfs()`へ、ノート別成分をそれぞれ`scaleTuningKfs(devNote)`・`NoteHist::swFineKfs`へ切り出し、3経路すべてが同じ内訳で計算するよう統一。**(4) 丸めの系統誤差**: RPN#1の14bit値→kfs換算が0方向への切り捨てだったため、指定値に対し常に最大1.56セント手前へ寄っていた。四捨五入に変更(kfsの粒度[1半音=64ステップ、約1.5625セント/step]が分解能の上限である点は変わらない)。`tests/test_midi_tuning.cpp`を新設し、CC#6→CC#38順でLSBが届くこと・パラメータ選択でMSB/LSBがクリアされること・CC#38単独では適用しないこと・RPN NULL中は無視されること・NRPNも同じ合成経路を通ること、および「fine=+64kfsがちょうど1半音」というkfs単位の契約を回帰テスト化した。**なお、Data Entry MSBの値はセント値ではない**(MSB=100は+100セントではなく約+56セント、MSB=1は+1セントではなく約−98セント)。この点はドキュメント側の記述も曖昧だったため、`midi-message-reference.md`にセント↔14bit値の換算式と対応表を追記した | ✅ | `midi-message-reference.md`, `midi-implementation-status.md` |
| SF2直行パスの解決済みパッチ名・実送出MIDIバイト列をMIDIモニターバンドへ表示(2026年8月、ユーザー要望「解決したパッチ名をBank/Prog列に、fluidsynthへ送ったNote Onメッセージ(生バイト列)をFnum列に表示してほしい」に対応)。`CFITOM::Sf2WindowState`(private)に表示専用キャッシュ(`lastProg`=最後にsub-cmd 0x05で送出したprog番号、`hasLastNoteOn`/`lastNoteOnStatus`/`lastNoteOnNote`/`lastNoteOnVel`=最後に送出したNote On[velocity>0]の生バイト列)を追加し、`routeSf2ChannelMessage()`の該当箇所(プログラムチェンジ送出時・Note On送出時)で更新するよう修正。読み取り専用スナップショット`CFITOM::Sf2WindowInfo`と`getSf2ChannelWindowInfo(mpu, ch)`を新設して公開した(ノート所有権・発音状態そのものは引き続き追跡しない、レジスタダンプモニターのシャドウレジスタと同じ「表示専用の最後の値」という位置づけ)。`Sf2BankRegistry`に`resolvePresetNameByIndex(soundfontIndex, sf2Bank, prog, outName)`(既存`resolvePresetName(cc32Bank, ...)`のCC#32解決を経由しない版、窓は既に`soundfontIndex`/`sf2Bank`をキャッシュ済みのため)を追加。`FITOMChannelMonitor`(`gui/bridge/FITOMBridge.h`)に`isSf2Windowed`/`sf2FluidsynthChan`/`sf2NoteOnHex`を追加し、`FITOMBridge::getChannelMonitors()`が窓に含まれる(mpu,ch)を検出すると`CInstCh`/`CRhythmCh`経由の解決を一切行わず、Bank列=SF2ファイル名(basename)、Program列=`resolvePresetNameByIndex()`で解決したプリセット名(未解決時は既存の数値フォールバック表示に自然に委ねる)、Device列=`"SF2 fluidsynth ch<N>"`、Fnumber列=送出したNote Onの生バイト列(16進2桁×3、例"90 3C 64"、ステータスバイトはfluidsynth chan付け替え後の値)を差し替える。`apps/fitom_gui/main.cpp`の`renderMonitorDataRow()`はFnumber列の描画のみ分岐を追加(`mon.isSf2Windowed`ならblock:fnumの代わりに`sf2NoteOnHex`を表示)、Bank/Program/Device列は既存の文字列フィールドをそのまま使うため変更不要だった。GUI表示のため既存のテスト基盤では検証できず、実機/実プラグイン接続環境でのユーザーによる目視確認が必要 | ✅(要目視確認) | `sf2-fluidsynth-integration.md` |
| CH設定ダイアログに、SF2直行パスの窓をチャンネル単位でON/OFFする切替インターフェースを追加(2026年8月、ユーザー要望「MIDI CH設定ダイアログに、SF2チャンネル切り替え(現行のリズムチャンネル切り替えと同じような)インターフェースを追加したい。SF2チャンネルではパッチピッカーはSF2バンクのみを表示する」に対応)。既存の「リズムチャンネル (CC#0)」チェックボックスと同じ操作感で、`ChSettingsDialog`に「SF2直行パス」チェックボックス+fluidsynth chan(0-15)スライダーを追加した。`isRhythm_`とは相互排他(ON時はリズム側を強制falseにしチェックボックスごと無効化、窓に含まれる限りメロディ/リズムの区別と無関係という設計のため)。初めて有効化した際は、`CFITOM::listAssignedSf2Windows()`(新設、全MPU×ch分の現在の窓割り当てを列挙する)から他の(mpu,ch)が使っていない`fluidsynth_chan`を自動算出して提案する。リズム切替と同じくOK確定(`applyAndClose()`)まで実際の送信は行わない(ライブプレビューなし)。パッチ選択は新設`Sf2PatchPickerDialog`(`apps/fitom_gui/Sf2PatchPickerDialog.{h,cpp}`)がBank(`banks.sf2_banks[].bank`、`FITOMBridge::getSf2BankList()`)→Program(`phdr`解決済みプリセット名つき、`getSf2BankPatches()`)の2階層を表示し、要望通り「SF2バンクのみ」に絞り込む。既存`PatchPickerDialog`のような押している間だけ再生する試聴機能は持たない: チェックボックスがOK確定まで窓を割り当てないため、ピッカーを開いた時点では試聴メッセージの送り先(SF2直行パスの窓)がまだ存在せず、既存のドラムキット選択ピッカー(試聴なし、Selectableで選ぶだけ)と同じ簡素な方式に揃えた。**この過程で発見した副次的な設計ギャップ**: GUIからの構造化送出(`MidiProcessor::sendControlChange/sendProgramChange/sendNoteOn/sendNoteOff`、`receiveByte()`を経由せずCFITOM経由で直接送る経路)が、実MIDI入力(`processMessage()`)と異なりSF2直行パスの窓判定(`routeSf2ChannelMessage()`)を一切経由していなかったため、修正(CFITOM.h宣言をインライン実装から前方宣言化し、CFITOM.cpp側でCFITOM本体定義より後ろに定義を移動。前方宣言のみのCFITOMに対しインライン関数内から`parent_->routeSf2ChannelMessage()`を呼ぶと「認識できない型」エラーになるため)。`Sf2BankRegistry`にGUI列挙用の`listBanks()`/`listPresetsInBank()`/`listPresetsByIndex()`、`CFITOM::Sf2WindowState`/`Sf2WindowInfo`に`lastCc32Bank`(CH設定ダイアログの初期値表示用)を追加。`FITOMChannelSettings`に`isSf2Windowed`/`sf2FluidsynthChan`/`sf2Bank`/`sf2Prog`を追加し、`FITOMBridge::getChannelSettings()`も窓判定を行うよう修正(以前は`getChannelMonitors()`のみ対応済みで、設定ダイアログ側は未対応のまま残っていた)。`tests/test_config.cpp`に`listBanks()`/`listPresetsInBank()`/`listPresetsByIndex()`の単体テストを追加。GUI変更のため既存のテスト基盤では検証できず、実機/実プラグイン接続環境でのユーザーによる目視確認が必要 | ✅(要目視確認) | `sf2-fluidsynth-integration.md` |
| OPLL系(`COPLL`/`COPLL2`/`COPLLP`/`COPLLX`/`CVRC7`/`COPLLRhythm`)で音程が意図通りにならないバグを修正(2026年8月、ユーザーから「OPLL系のFnum算出が怪しい」との報告で発覚)。真因は`chip-driver-architecture.md`に既に記録されていた2026年7月のOPL系バグ(コンストラクタが`fnumMaster`[実機マスタークロック]に、呼び出し元から渡される`sampleRate`[オーディオのサンプルレート、44100等。`profile.schema.json`上も「HWプラグインの遅延キュー計算用」と明記されている値であり実機クロックとは無関係]をそのまま使ってしまい、Fnumber計算式`freq*(2^17/master)*divide`の結果が常に65535[uint16_t上限]にクランプされる)と全く同一のパターンが、当時「OPL系だけの誤り」と判定されOPLL系は見落とされていたもの。`tests/test_soundevice_freq.cpp`の`COPLLRhythm`テストが`createCOPLLRhythm(&port, 3579545)`と正しいクロック値を直接渡して書かれていたため、テストではこの不具合が症状として現れていなかった。OPLLはOPL系と同じ3.579545MHz(NTSC colorburst由来)・`divide=72`の共通ファミリーのため、`OPLL_new.cpp`に`kOpllMasterClock=3579545`を新設し、`COPL`と同じ修正パターン(コンストラクタの`sampleRate`引数はファクトリ関数シグネチャ一貫性のためだけに残し、Fnum計算には使用しない)を適用した | ✅ | `chip-driver-architecture.md` |
| OPLL系(メロディパート・ビルトインリズムの両方)で音量の極性が反転していたバグを修正(2026年8月、上記Fnumバグ修正の実機確認中にユーザーが「Vol最大で無音、最低で最大音量になっている」と報告し発覚、さらにユーザーの追加依頼でビルトインリズム側も確認し同種のバグを発見)。**(1) メロディパート**: `COPLL::updateVolExp()`/`updateTL()`が、既にラウドネス空間(0=無音,127=最大音量、`OPN_new.cpp`の`effTLToReg()`コメント参照)で返る`effectiveTL()`/`lev`に対し、不要な`127 - x`反転をさらに適用していたため二重反転で極性が逆になっていた。OPL/OPN/OPMの対応箇所(`effTLToReg()`)は反転なしで直接`linear2dB()`に渡す設計であり、これが正しいパターン。`docs/STATUS.md`に記録済みのADPCM-A/ADPCM-Bの`127-effectiveTL()`誤反転バグと全く同じ移植ミスのパターンだった。両メソッドから`127 - `を除去。**(2) ビルトインリズム**: `COPLLRhythm::updateVolExp()`の`calcVolExpVel(s.volume, 127u, 127u - s.velocity)`も同様に、`calcVolExpVel(vol, exp, vel)`の3引数が全て同じラウドネス空間(高いほど大きい音、`OPL4.cpp`/`PSG_new.cpp`の`calcVolExpVel(s.volume, s.expression, s.velocity)`呼び出しと同じ規約)であるにも関わらず、旧FITOMの`CalcLinearLevel(vev, tl)`(こちらは第2引数が減衰量空間で`127-velocity`変換が正しかった)からの移植時に同じ変換式をそのまま持ち込んでしまい、強く叩くほど静かに・弱く叩くほど大きく鳴る逆転になっていた。`127u - s.velocity`を`s.velocity`に修正(OPL系のビルトインリズム`COPLRhythm`は`calcVolExpVel`を使わずメロディパートと同じ`effectiveTL()`経由の設計のため、この問題は無い) | ✅ | — |
| パッチピッカーでOPLLビルトインROM音色(バンク0固定)・OPNA/OPLLビルトインリズム(CC#0=0x70)が常に空欄で表示される不具合を修正(2026年8月、AWMバンク欠落バグ[上記表参照]の修正を報告した際、ユーザーから「OPLLのビルトインパッチおよびOPLL/OPNAのビルトインリズムパッチも同様に手当てが必要では」との指摘を受けて着手)。両者とも通常のHwBankRegistryを一切経由しない特殊解決経路(`PatchManager::resolveOpllRomVoice()`/`resolveBuiltinRhythm()`)のため、AWMと全く同じ理由でGUI側(`FITOMBridge::getHwBankList()`/`getHwBankPatches()`/MIDIモニターの`getChannelMonitors()`)が列挙できていなかった。**OPLL ROM音色**: `PatchManager::getOpllRomPatches(voicePatchType)`(カテゴリ単位で16音色を返す、ピッカーのバンク/プログラム一覧向け)と`getOpllRomPatchByProg(hwProg)`(`resolveOpllRomVoice()`と全く同じデコード規則でhwProgから直接1件解決する、MIDIモニターのパッチ名表示向け)を新設し、バンク0を「ROM Preset」として合成表示するようにした。**実装中に発見した副次バグ**: `resolveOpllRomVoice()`は渡されたCC#0(voicePatchType)の値ではなく、hwProg自身の上位3bitで実際のチップ種別(variantSel)を再デコードする設計のため、ピッカーがバンク一覧の配列添字(instIndexのみ)をそのままProgram Change値として送ると、OPLL以外のカテゴリ(OPLLX/OPLLP/VRC7)でvariantSelがずれて別チップの音色が鳴ってしまう(音は出るが選んだものと違う)不具合を実装時に作り込みかけた。`HwPatch::id`(variantSel<<4|instIndex、初期化時に既に正しくエンコード済み)をそのままProgram Change値として使うよう修正し、`tests/test_config.cpp`にこのエンコードとhwProgからの直接解決を検証する回帰テストを追加した。**内蔵リズム**: CC#0カテゴリ一覧に`0x70`を追加し、バンクレベルに対象チップ(OPNA/OPLL、`resolveBuiltinRhythm`の`chipSel`引数と同じ値)、プログラムレベルに各チップの内蔵リズムパート名(実機固定、OPNA=6パート[Bass Drum/Snare Drum/Top Cymbal/Hi-Hat/Tom/Rim Shot]、OPLL=5パート[Hi-Hat/Top Cymbal/Tom/Snare Drum/Bass Drum]、`DeviceFactory::defaultChCount()`のchCountと一致)を列挙するようにした。両チップでパート数が異なるため、共通化せずチップごとに別々の固定配列として実装している(ユーザーからの確認質問を受けて明示的に検証済み) | ✅ | `patch-structure-design.md` |
| OPLL内蔵リズムのスネアドラム(SD)が、起動直後は鳴るが途中から鳴らなくなる不具合を修正(2026年8月、ユーザーが実機のレジスタダンプ動画2本[起動直後/現象発生時]を比較し、`$37`の下位nibbleが`0x0`→`0xC`(12ステップ減衰、dB比較で35.7dB≒12×3dBと一致)に変化していること、`$0E`のSDキーオン自体は両方とも正しく発行されていることから発音経路ではなく音量nibbleのみの問題と特定して報告)。`COPLLRhythm::updateVolExp()`の`bool highNibble = (ch & 5) != 0`が、旧FITOM(`legacy/src/OPLL.cpp`の`COPLLRhythm::UpdateVolExp`、`mask = (ch&5) ? 0xf0 : 0x0f`)と符号が逆転していた。SDは自分では`$37`下位に一度も書き込まれず起動直後は`0`(最大音量)のままだが、ハイハット(HH)を1回でも叩くと、本来HH専用のはずの`$37`上位ではなく下位(=SDの音量欄)にHHの音量が誤って書き込まれ、以後SDの音量がHHの音量のまま固定的になっていた(HH自身のキー入力毎にのみ更新されるため、SD単体を叩いても音量が変わらない="鳴らなくなった"ように聞こえる)。同一原因の副作用として、BDのベロシティが効かない(`$36`上位はリズムモードで未使用、下位は常に`0`=最大音量のまま)、TOMとCYMの音量が入れ替わる、も併発していた。`(ch & 5) != 0`を`(ch & 5) == 0`へ修正(正しい対応: HH→`$37`上位/CYM→`$38`下位/TOM→`$38`上位/SD→`$37`下位/BD→`$36`下位)。**OPL系(`COPLRhythm`)の確認**: OPLはTLが楽器ごとに完全に独立した1バイトレジスタ(`0x40+opIdx*3+slot`、2命令が同じ物理chを共有してもop0/op1で異なるアドレスに書かれる)であり、OPLLのような「1バイトを2命令でnibble分割共有」という構造自体が無いため、同種の問題は無いことをコード確認した。`tests/test_soundevice_freq.cpp`に、HH/SD($37)・CYM/TOM($38)がそれぞれ正しいnibbleに独立して書き込まれること、BD($36)が下位nibbleのみ使うことを検証する回帰テストを追加(修正前のコードに対して実行し期待通り失敗することも確認済み) | ✅ | — |
| ソフトウェアLFOをワンショット(one-shot hold/zero)に設定しているのに、のこぎり波が1周期のうちに2回繰り返して聞こえる不具合を修正(2026年8月、ユーザーがドラムノート編集の試聴で`DrumLFO Strong`(`LWF=4`下降のこぎり波/`LFM=1`/depth 600cent)を鳴らして報告)。直前のコミット[のこぎり波を半周期分位相シフトし、発音開始時にピッチが飛ばないよう変位0から始める変更]によるリグレッション。位相シフトをrepeat/one-shotの区別なく適用していたため、one-shotの1周期が「変位0 → -120 →(不連続点で+120へ跳ね上がり)→ 変位0」となっていた。repeatなら不連続点が周期のどこにあっても位相オフセットの違いでしかないが、one-shotの1周期はそのままピッチ/音量エンベロープになるため、周期内の跳ね上がりが「1回しか鳴らないはずのLFOの2回目」として聞こえていた(さらにホールド値が-120固定だったため、周期末尾[≈変位0]からホールドへ移る瞬間にもう一段跳んでいた)。`LfoControl::wave()`(`VoiceProcessor.cpp`)で、repeatは従来通り半周期シフト(振幅±120)、one-shot hold/zeroは不連続点のない片側傾斜(`0 → ±120`を単調走査、周期末尾の値がそのままホールド値±120へ連続する)と、モードで位相の扱いを分けた。`tests/test_voiceprocessor_lfo.cpp`にワンショット1周期の単調性(up-sawは単調増加/down-sawは単調減少)とホールド値の連続性の回帰テストを追加 | ✅ (実機確認済み) | `voice-data-design.md`, `manuals/swpatch-reference.md` |
| ドラムノート(`CRhythmCh`)でのみ、ソフトLFO/トレモロが2倍速・2倍デプスになり`DrumNote.fineTune`(微調整)が無効化される不具合を修正(2026年8月、上記のワンショットLFO調査中に発覚)。`CFITOM::timerCallback()`はMIDIプロセッサ(→`CRhythmCh::timerCallback()`)とデバイス(→`CSoundDevice::timerCallback()`)の両方を毎tick呼ぶが、`CRhythmCh`側が独自に`ChState::proc.onTick()`を回していたため、同じ`VoiceProcessor`へ二重にtickが掛かっていた(LFOが2倍速)。加えて、その変調結果の反映に`setNoteFine(devCh, note, channelLfoValue())`を使っていたため`ChState::fineFreq`がLFO値で上書きされ、(a)`applyNoteOn()`が書き込んだ`DrumNote.fineTune`が最初のtickで失われる、(b)`CSoundDevice::getFnumber()`が`fineFreq + channelLfoValue()`を足すためLFOのデプスが2倍になる、という2つの副作用があった。`ChState`へのhwPatch/swPatchキャッシュ(2026年7月新設、`buildVoiceForCh()`)が入った時点で`CRhythmCh`側の独自ループは冗長になっており、`CSoundDevice::timerCallback()`が`onTick()`→`updateFnumber()`/`updateTL()`まで面倒を見るため、`CInstCh`と同じくデバイス側へ一本化した(`CRhythmCh::timerCallback()`はゲートタイムのカウントダウンのみに縮小)。副次的に、`CRhythmCh`側のループが`rl->hwPatch`の無いレイヤーをスキップしていたためソフトLFOが一切効いていなかったサンプルベース音源系(`usesVoiceProcessorForSamplePatch()`がtrueのチップ)のドラムレイヤーも、デバイス側の経路に乗ることで対象になる。タイマー経路が絡み既存のテスト基盤で決定的に再現するユニットテストは追加していない(コードレビュー・既存テスト全件PASSで検証) | ✅ (実機確認済み) | `chip-driver-architecture.md` |
| ソフトウェアLFOを指定していない(`sw.LFR=0`、CC#1も未送信)チャンネル/パッチにソフトウェアLFOが掛かる不具合が再発(2026年8月、上記CC#1修正[`isAssigned()`追加]の効果検証中に報告。今回はドラム/リズムパートとメロディパートが同一チップを共有する構成で発生)。原因は`CRhythmCh::applyNoteOn()`(`MidiCh.cpp`)。`CInstCh::noteOn()`と違い`ISoundDevice::setCC1Modulation()`/`setLfoDepthOverride()`を一切呼んでいなかった(`CRhythmCh`はCC#1/CC#77というコントローラ自体を扱わない設計のため)。この結果、「CC#1でソフトLFOをかけたメロディch」→「そのdevChを奪うドラムノート(何もpushしないので残留状態がそのまま素通りする)」→「同じdevChを奪う別のメロディch(CC#1未送信)」の順でdevChが使い回されると、`VoiceProcessor::cc1Value_`の残留がドラムノートをすり抜けて2つ目のメロディchまで生き残ってしまっていた(`tests/test_soundevice_freq.cpp`のユニットテストでこの3ステップの経路を直接再現し、修正前は実際に失敗することを確認済み)。`CRhythmCh::applyNoteOn()`にも`CInstCh::noteOn()`と同じ「毎ノートオン強制push」パターンを追加(`CRhythmCh`はCC#1/CC#77の値を保持しないため、常に無効値`setCC1Modulation(devCh, 0, 0)`/`setLfoDepthOverride(devCh, 0)`をpushする。後者の無効値は本コミット時点では`-2000`だったが、下記のCC#76-78オフセット方式への変更に伴い`0`へ変わっている)。`devCh`の使い回しがチャンネル種別(`CInstCh`⇔`CRhythmCh`)をまたいでも残留状態が上書きされるようになった | ✅ | `midi-implementation-status.md` |
| 上記の修正後もなお「ソフトウェアLFOを指定していないチャンネル/パッチにLFOが掛かる」症状が再発したため再調査(2026年8月)。今回はポリフォニックチャンネルで発生しており、上記のモノ自己スティール・`CRhythmCh`経由の漏れとは無関係と判明。実機ログ(`CInstCh::noteOn()`内に一時デバッグログを仕込んで採取)により、パッチ本来の解決結果(`rl->swPatch`)は正しく`LFR=0`なのに、実際にassignCh/allocChへ渡される`effSwPatch`だけが`LFR=64`になっていることを特定。真因は`CC#76`(Sound Controller 7、ソフトウェアLFO Rate上書き、非標準)。当時の実装は受信値(0-127)を`sw.LFR`へ**そのまま絶対上書き**する方式だったため、GM2準拠のDAW/MIDIファイルが演奏開始時にSound Controller群(CC#70-79)を既定値**64**へリセットする定型初期化を送ると、FITOM_Xはこれを「Rateを64に強制指定」と誤解釈し、音色データ側が`sw.LFR=0`(LFO不使用)に設定していても構わずLFOを起動していた(実機ログでMIDI CH1にCC#71-75=64のリセット定型句が確認でき、CC#76も同じ初期化バーストに含まれていたと判明)。CC#77(Depth)も同じ絶対上書き設計だった。GM2規格のSound Controllerは本来「値64=中央(補正なし)」のオフセット方式であり、FITOM_Xの実装がこれに従っていなかったことが根本原因。`CInstCh::lfoRateOverride_`/`lfoDelayOverride_`/`lfoDepthOverrideCents_`を全て「音色データの値へ加算するオフセット」(0=補正なし)として扱うよう再設計し、`CInstCh::noteOn()`の焼き込みロジックと`VoiceProcessor::recalcChLfo()`の両方を絶対上書きから加算方式へ変更した。これによりGM2準拠のリセット初期化(値64)は自動的に無害(オフセット0)になる。`tests/test_voiceprocessor_lfo.cpp`にCC#77相当のオフセット方式(0=変化なし、正負でデプスが増減し符号も反転しうる)を検証する回帰テストを追加(CC#76/78の焼き込みロジックは`CInstCh::noteOn()`内にあり軽量に単体テストできる箇所ではないため、コードレビューと既存テスト全件PASSで検証)。調査に使った一時デバッグログはすべて削除済み | ✅ | `midi-implementation-status.md`, `manuals/midi-message-reference.md`, `manuals/swpatch-reference.md` |
| リニアステレオ化(`CLinearPanDevice`)がcomposite展開されるチップで一切発動しなかった不具合を修正(2026年8月、ユーザーがOPLL×2台のステレオ構成プロファイルを作成し「単にスパニングされるだけでステレオにならない」と報告して発覚)。原因は2点。①`FITOMConfig::pushDeviceEntries()`のcomposite分岐が`stereo_pair`を「複雑になるため現時点では非対応」として警告のみで捨てており、`DeviceEntry::stereoPairRequested`を設定すらしていなかった。`rhythm_mode:true`のOPLL/OPL系やOPNA系など、sub-device自動生成の対象になるチップは全滅で、この機構が実際に使えるのは単独チップ(OPLLならVRC7のみ)に限られていた。②`mergeStereoPairDevices()`のペアリング判定が`VoicePatchType`基準だったため、専用の`VoicePatchType`を持たない内蔵リズムサブデバイス(`COPLLRhythm`/`COPNARhythm`は`VOICE_PATCH_NONE`)が常に対象外になり、仮に①を直してもR側のリズムだけが孤立して残る作りだった。対処として、①composite分岐でも`stereoPairRequested`を全サブデバイスへ伝播し、②判定基準を`deviceType`一致(CLinearPanDeviceは物理的に同一チップ2台が前提のため`VoicePatchType`より厳しいこちらが本来の意味に合う)へ変更した上で、composite展開されたエントリ群は`compositeGroup`単位・全サブデバイスの1対1対応が成立する場合に限りまとめてペアリングするようにした。ペアリング本体は`FITOMConfig::pairStereoDevices()`(static)へ切り出した。なお伝播は無条件ではなく、`FITOMConfig::subDeviceAcceptsStereoPair()`が`false`を返すサブデバイス(現状はOPL4のAWM/PCM部`DEVICE_OPL4AWM`のみ)は対象外とする(ユーザー指摘。AWM部はチャンネルごとのパンポットをハードウェアで持っており、L/R2台の束ね[モノラル出力しか持たない音源に対する代替手段]を適用すると本来のパンポットを潰した上でチップを2倍消費することになる)。これに伴いcompositeグループのペアリング判定も「グループ全体の一致」ではなく「stereo_pair対象サブデバイスの部分集合の一致」とし、構成の異なるグループ同士(OPL4のFM部 と OPL3、どちらもAWM部を除けば`DEVICE_OPL3`+`DEVICE_OPL3_2`)をL/Rペアとしてステレオ化する用法が成立するようにした。`tests/test_config.cpp`に回帰テスト8件(stereo_pair無指定/片側のみ指定では発動しない、単独チップのペア、composite群のペア、別チップ・別I/Fは束ねない、独立した2ペアの取り違えなし、OPL4AWMのみ除外される、OPL4のFM部とOPL3のペア+AWM独立、OPL4同士のペアで両側のAWMが独立)を追加 | ✅ | `chip-driver-architecture.md`, `config-design.md` |
| リニアステレオ化に「チップ内L/R分離方式」(`stereo_pair:"L"/"R"`)を追加(2026年8月、ユーザー提案)。従来方式(`stereo_pair:true`)は`pan`がL/R役割の宣言とプラグイン側の出力ルーティングを兼ねており、`pan:1/2`を指定した時点でそのチップの出力全体が左右どちらかへ寄るため、同一物理チップ上の他のサブデバイス(OPL4のAWM部等)が持つハードウェアパンポットを潰してしまう問題があった。新方式では`stereo_pair`に`"L"`/`"R"`を書いてL/R役割を`pan`から独立して宣言し、左右の分離は`CLinearPanDevice`が束ねた2チップ自身の`ChState::panpot`を左右端(-64/+63)へ固定することでチップのL/R出力ビット(OPM:`0x20` bit7/6、OPL3:`0xC0` bit5/4、OPN2/OPNA:`0xB4` bit7/6)に行わせる。プラグイン側の`pan`は`0`(Stereo)のままでよくなるため、OPL4のFM部をL/Rペアにしつつ**AWM部が15段のハードウェアパンポットをフル活用できる**。負値=L/正値=Rはドライバ共通の規約であることを全ドライバの`updatePanpot()`で確認済み。対応可否は`FITOMConfig::deviceHasChipLevelPanpot()`が`deviceType`側の索引として持ち(真の情報源は各ドライバの`updatePanpot()`実装)、非対応チップ(OPLL系・OPL/OPL2・SSG/PSG系・YM2203)に指定した場合は読み込み時に警告する(黙って左右に分離されない静かな失敗の防止)。OPNAのSSG部のようにチップ内にL/R分離手段が無いサブデバイスはINFOで通知するに留める(実機の制約。ペア内の全サブデバイスが非対応のときのみWARN)。片側だけ明示・片側はpan由来という混在指定は従来方式に倒す。`tests/test_config.cpp`に回帰テスト5件を追加 | ✅ | `chip-driver-architecture.md`, `config-design.md` |
| リニアステレオ化が実際に発動するようになった直後、実機で「束ねられてはいるがDVA制御に失敗し`CUnison::allocCh: no channel available`が大量発生、演奏が正しくない」と報告され、`CUnison`の潜在バグ2件を修正(2026年8月)。①`CUnison::queryCh()`の事後チェックが、代表チップ`chips_[0]->queryCh(mode)`の戻り値に対し**modeに関わらず全チップに`isEmpty()`を要求**していた。`findBestCh()`はmode=1でEmpty**またはReleasing**を、mode=0ではさらにRunning(強制奪取)を返す設計のため、代表チップが正当に返したchがこの事後チェックでほぼ必ず弾かれ、「全チップで完全にEmpty」のときしか発音できなかった。減衰中/発音中のchが残る通常の演奏ではほぼ常に割り当て失敗となりノートが落ちていた(旧FITOM`CUnison::QueryCh`は`chips[0]`の結果をそのまま返しており、この事後チェック自体が新実装で追加されたもの)。判定基準をmodeと一致させる(mode=1→Empty/Releasing、mode=0→Disabledでなければ可)ことで修正。②`CUnison::noteOn()`が無条件に`ChState::fineFreq`へ±8kfs(=12.5cent)のデチューンを**代入**していた。`CLinearPanDevice`は物理的にL/Rへ固定配線された同一チップ2台を1つのステレオデバイスとして扱う構成であり、左右で音程がずれると定位ではなくビート(うなり)になるうえ、直前に`setNoteFine()`が書いた音色側のfine tune(`sw.fine_tune`・マスターピッチ等)を上書きで失っていた(`MidiCh.cpp`の発音経路は`setNoteFine()`→`noteOn()`の順)。`CUnison`にデチューン量パラメータ(既定8kfs)を追加し、`CLinearPanDevice`は0を渡して`fineFreq`に一切触れないようにした。`tests/test_soundevice_freq.cpp`に回帰テスト2件(Releasing/Running状態でもallocChが成功し続けること、noteOnがL/R間で音程をずらさずfine tuneも保つこと)を追加 | ✅ | `chip-driver-architecture.md` |
| リニアステレオ化ペアのR側ポートに`resolveHighBankPort()`が適用されていなかった不具合を修正(2026年8月、レベルメーター対応の実装中に発見)。L側は`CFITOM::initDevices()`側で高位バンクへの差し替え済みだったが、R側は`createLeveledDevice()`へ生ポートのまま渡されていたため、ADPCM-A/ADPCM-B(OPNA)のように自分専用の高位バンク(0x100以降)へ配置されるサブデバイスをステレオペアにすると、R側だけ低位バンクへ書き込んでいた(OPL4のAWM部は`subDeviceAcceptsStereoPair()`で束ね対象外のため該当しない)。実害が出るのはOPNA/OPNB系を`stereo_pair`にした構成 | ✅ | `chip-driver-architecture.md` |
| レジスタダンプモニターの表示アドレス範囲をチップごとの実レジスタ空間に合わせた(2026年8月、ユーザー要望)。従来は`PhysicalChipInfo::dumpSize`の初期値0x100が下限として効いており、OPLL(有効レジスタ0x38まで)やSSG(0x0Dまで)のように1ポート分に満たないチップでも常に0x000-0x0FFの16行が出て、大半が0埋めの空行だった。レジスタ空間サイズのテーブルを`kDevMap`の`regsize`列から専用の`kRegSizeMap`(`CFITOM.cpp`)へ分離し、`DeviceFactory::create()`のswitch単位で全チップドライバ分の値を16byte境界切り上げで登録。`buildPhysicalChipList()`は最大値採用のあと16byte境界へ正規化し、未登録`deviceType`しか無い物理チップのみ0x100をフォールバックとして出す | ✅ | `chip-driver-architecture.md` |
| HwBankの登録キーをチップ族(VoiceGroup)単位からチップ種別(VoicePatchType)単位へ変更(2026年8月、ユーザー提案)。バンク番号の名前空間がOPM/OPZ/OPZ2、OPL/OPL2/OPL3_2、OPLL/OPLLP/OPLLX/VRC7といった族単位で共有されていたため、プロファイルの`hw_banks[]`に`group`だけ変えた同一バンク番号のエントリを並べても同じスロットを奪い合い、最後に読み込まれたエントリがバンクのチップ種別タグを上書きしていた(結果として、そのバンクは1つのCC#0値からしか引けなかった)。`HwBankRegistry`を`SampleZoneBankRegistryと同じ`(voicePatchType, bankNo)`の二重mapキー方式に変更し、同じバンクを複数のチップ種別へ登録して任意のCC#0値から選べるようにした。PSG系(SSG/EPSG/DCSG/SAA/SCC)は族全体で1つのバンク名前空間を共有する既存設計を維持するため、`PatchManager::hwBankLookupVoicePatchType()`が検索キーを`VOICE_PATCH_SSG`へ寄せる。これに伴い`resolveTriple()`のバンク側タグ一致チェック(構造的に常に一致するため不要になった)と、未使用だった`HwBankRegistry::groupFromDeviceId()`を削除。`FITOMBridge`のバンク列挙・パッチ列挙・モニター名解決も同じキーに揃え、「一覧には出るが選ぶと解決に失敗する」非対称を解消した | ✅ | `patch-structure-design.md`・`midi-message-reference.md` |

---

## 既知の未対応・将来課題

- SF2直行パス(fluidsynth統合)は、このリポジトリのスコープであるFITOM_X本体側(設定読み込み・MIDIディスパッチ・プライベートSysEx・プリセット名解決用`phdr`パース・MIDIモニターバンドへの表示配線・CH設定ダイアログでのチャンネル単位の窓切替)は実装済みだが、実際に音を鳴らすhwifプラグイン本体(仮称`FitomSf2IF`、別リポジトリ、`fluid_synth_t`をリンクする側)は未実装。GUI側(MIDIモニターバンドの表示・CH設定ダイアログの新規UI)は実機/実プラグイン接続環境での目視確認が未実施。また`sf2_channel_windows`(複数チャンネル分)を一覧編集する専用ダイアログも未着手(チャンネル単位の切替はCH設定ダイアログで対応済みのため優先度は低い、`docs/sf2-fluidsynth-integration.md`5節参照)
- Poly Pressure / Channel Pressure
- CC#67 Soft Pedal（FM音源に対応するパラメータがないため意図的に非対応）
- RPN 0x0002 Coarse Tuning、RPN 0x7F7F Null
- CC#2/CC#4 の変数分離
- VoicePatchType 未実装チップ (MA3系列, SAA1099, AWM) のドライバ実装
- VoicePatchType 完全一致以外へのフォールバック（旧FITOMの互換リスト相当、将来実装予定）
- GUI (Dear ImGui) 実装の残り(`apps/fitom_gui`。MIDIモニターバンドは実装済み。デバイス一覧・パッチ一覧・音色エディタ等、他画面への導線が未着手)
- 外部パッチエディタ起動機能(ダブルクリック→キオスクモード起動)は実機でのダブルクリック確認済み(2026年7月、プロファイルパス解決バグ修正、上記表参照)。ただし`kind="device"`(直接モード)の解決経路(`pm.resolveDirect()`)は`PatchManager::resolveTriple()`がVoiceGroup照合のために`devices[]`から一致するデバイスを線形探索する都合上、プロファイルの`devices[]`に対応するHWプラグインDLLが実際にロードできていないと(`Config::buildDevice()`がHWプラグイン未登録時に早期returnし`DeviceEntry`自体を作らないため)解決に失敗する点は未検証のまま残っている(`kind="layered"`側は`pm.findPatchBank()`のみで完結するためこの制約を受けない、2026年7月のkind対応時に判明)。4引数キオスク起動(`<profile> <kind> <bank-file> <prog>`、パッチエディタ側D-039/D-040)自体の実機動作確認(実際にlayered/deviceそれぞれで適切な編集画面が開くこと)は未実施
- 外部パッチエディタとの内部用MIDIパイプ(`fitom_midi_pipe`)連携: 実機MIDIキーボード(rtmidi)との同時併用不可という設計上の制約は解消済み(2026年7月、プロファイル設定から独立して常時有効化するよう変更、上記変更履歴参照。`-DFITOM_BUILD_BACKEND_MIDI_PIPE=ON`でビルドすれば追加のプロファイル設定なしに常時有効)。PowerShellの簡易クライアントによる疎通確認(チャンネル割り当て通知・Program Change・Note On/Offの送受信)は実施済みだが、実際の外部パッチエディタプロジェクト(`FITOM_patch_editor`)との実接続確認は引き続き未実施
- GUI MIDIパイプ経由の音色試聴連携(`fitom_midi_pipe`側は実装済みだが、GUI側からのSysEx送出・パッチエディタ本体との結合は未着手)
- **OPM/OPZ ハードウェアLFO対応: 設計検討完了・実装未着手(2026年8月)**。現状の配線と、実装に着手する際の設計方針を以下にまとめる。
  - **現状の配線**: 感度側は配線済みで、`CInstCh::noteOn()`が`enablePM`/`enableAM`を無条件にtrueで呼び、`COPM`がパッチの`hw.AMS`/`hw.PMS`をreg`$38+ch`へ書く。発生器側はCC#14/#15(`hwLfoDepth_`/`hwLfoRate_`→`setLFODepth`/`setLFORate`)経由のみで、初期値0のためCCを送らない限り無音変調になる。LFO波形(reg`$1B`)はパッチにもCCにも無く、`$1B`への書き込み自体が存在しないため常に波形0固定。OPZの第2LFOは`COPZ`が`COPM`を継承するだけで未実装(旧FITOMも未実装)
  - **パッチ側フィールド**: `FmHwVoice`が持つHW LFO関連は`AMS`(2bit)/`PMS`(3bit)と、オペレータ単位の`FmHwOp::AM`(1bit)のみ。`LFRQ`/`PMD`/`AMD`/波形に相当するフィールドは**意図的に持たない**(チップ内蔵LFOがチップ全体で1系統の共有リソースであり、マルチティンバー・ダイナミックボイスアサインと相性が悪いため。`voice-data-design.md`「旧`LFO`フィールドは廃止」の項参照)
  - **OPZレジスタ仕様**(出典: <http://sr4.sakura.ne.jp/fmsound/opz.html>、TX81ZのシステムROM解析): `$16`=LFRQ2 / `$17`=AMD2(b7=0)・PMD2(b7=1) / `$18`=LFRQ / `$19`=AMD・PMD(同b7) / `$1B`=CT(b7-6)・SY2(b5)・SY(b4)・LW2(b3-2)・LW(b1-0)。`$38+ch`は**b7がPMS/PMS2のセレクタ、b2がAMS/AMS2のセレクタ**で、値はb6-4(PMS側)とb1-0(AMS側)。1回の書き込みでPM側とAM側の窓を独立に選べる
  - **設計方針(結論)**: パッチにAMS2/PMS2を追加する必要は無い。実機TX81Zも**LFO選択はボイスデータではなくパフォーマンスデータ側のチャンネル単位パラメータ**(`{off/LFO1/LFO2/Vibrato}`、うちVibratoはソフトウェアによるピッチモジュレーション=FITOM_Xの`sw.LFR>0`ソフトLFOに相当)であるため、FITOM_Xも**明示選択のパフォーマンスパラメータ**として持たせる。これによりLRU/スチール等の自動割り当て器は不要になり、動作が決定的になる。リソース競合は「同じインスタンスを選んだMIDIチャンネル間では後発優先」で解決し、`lfoCount_==1`のOPM/OPPでは現行動作にそのまま縮退するためチップファミリー間の一貫性も保てる
  - **着手前に修正が必要な既存の不具合**: (1) `COPM::enablePM`/`enableAM`が`$38+ch`をread-modify-writeしている(`(getReg(...) & 0x07) | ...`)。保持マスク`0x07`にAMS側セレクタb2が含まれるうえ、`getReg()`が返すシャドウ1バイトでは窓ごとに別値を持つ`$38+ch`を表現できないため、OPZでは必ず破綻する。ドライバ側に8ch分の`{PMS, AMS, PMS2, AMS2}`を持ちフルバイトを組み立てて書く方式に変える(`$40`/`$C0`の窓切り替えは`COPZ::updateVoice`が既にこの方式で正しい)。窓の切り替え時は旧窓へ0を書くクリアが別途必要で、怠るとそのチャンネルが両方のLFOを受ける。(2) `COPM::reset()`のレジスタクリアが`$20`〜`$FE`でLFOレジスタ`$18`/`$19`を含まない一方、`lfoDepth_`/`lfoRate_`キャッシュは0に戻るため、`setLFODepth`/`setLFORate`の同値早期リターンと組み合わさって実チップに古い値が残る。`init()`での`$1B`初期化も同様に必要(`getReg($1B)`のRMWがCT(b7-6)を壊さないようにするため)
  - **CC割り当て案**: CC#14/#15(Depth/Rate、既存)は「そのMIDIチャンネルが選択中のインスタンス」へ書く解釈に変更し、CC#20=LFO波形(0-3)、CC#21=LFO SELECT(0=off/1=LFO1/2=LFO2)、CC#22=キーオン同期(`$1B`のSY/SY2、任意)を新設する。CC#16-19はGM1で"General Purpose Controller"の名前が付くため避け、完全な未定義域を使う
  - **未確認事項**: FitomEmuIF(YMEngine)側がOPZの第2LFO(`$16`/`$17`)、`$1B`のLW2/SY2、および`$38+ch`のセレクタビットb7/b2をエミュレートしているか。特にセレクタビット未実装の場合、AMS2/PMS2への書き込みがAMS/PMSを上書きする形で誤動作する。あわせて`COPZ::init()`が資料のシステム初期化のうち`$1C`/`$1E`への`$00`書き込みを行っていない(資料でもLFO関係と推測されている番地)
  - **実装順**: ①`$38`のRMW廃止 + `reset()`/`init()`のLFOレジスタ初期化(現行動作を壊さない土台固め) → ②LFO1の波形/SELECT CC追加 → ③OPZ第2LFO
- CAdPcmZ280 (YMZ280B/PCMD8) の旧FITOM実装との詳細突き合わせ未完了
- レジスタダンプモニター(`RegisterDumpWindow`)の基本動作(発光エフェクト・オルタネート表示・物理チップ名表示・高位ポート表示含む)はユーザーによる実機/実プラグイン接続環境での目視確認とフィードバックを経て、単一物理ポートで高位アドレス(0x100以降)を使うチップ(OPNA/OPN2/OPL3等)の高位側が表示されない不具合(`PhysicalChipInfo::dumpSize`新設で修正)、チップごとのテーブルに常にスクロールバーが出る不具合(テーブル高さ固定+ScrollYをやめ自動サイズ化で修正)の2件を修正済み。後者の修正自体は、この開発環境にHWプラグインDLL・物理チップが無くビルド確認までしかできていないため、ユーザーによる目視再確認が未実施
- AWM(YMF278)のSwPatch(パフォーマンスパッチ)非対応: `zones[].sw_bank/sw_prog`・`pcmbank.swpatches[]`の参照自体は解決されるが、`COPL4AWM`は音量計算・`onNoteOn()`呼び出しの対象外のままのため音には反映されない。実機がトレモロ・ビブラートをレジスタ`0x80-0x97`のLFO/VIBビットとしてデバイス機能に持ち、`samplezone`が参照する波形バイナリ側の設定と整合させる設計が必要なため(2026年7月、`patch-structure-design.md`参照)
- VGMレコーディング機能(GUIから任意開始・停止、最初のレジスタ書き込みから記録、停止時にファイナライズ)は技術検討完了・実装未着手(2026年7月、`docs/vgm-recording.md`参照)。`HWPort::write()`/`writeBurst()`(既存のシャドウレジスタ更新と同じチョークポイント)へのフック、`CFITOM::PhysicalChipInfo`(レジスタダンプモニター用の物理チップ列挙)を流用したヘッダ構築、壁時計ベースのwaitサンプル変換、`PcmBankRegistry`の`binPath`を使ったROMイメージdata block埋め込みという設計方針は確定しているが、コード実装(`VgmRecorder`新設、`FITOMBridge`のAPI追加等)には未着手
- チャンネルレベルメーター(`LevelMeterPanel`)は実機確認により、同種デバイス自動束ね(spanGroups、例: OPN2とOPNAのFM部が同一VoicePatchTypeのため自動的に束ねられる構成)が絡む物理チップで表示が大きく欠落する不具合が発見され修正した(2026年7月)。原因は`buildPhysicalChipList()`がspanGroupsで他デバイスに吸収された側・吸収した側の両方をサブデバイス内訳の対象外にしていたため。`CFITOM::initDevices()`のデバイス構築ループに`pendingSubDevices_`(span/stereo展開“前”の生ISoundDevice記録)を新設し、`buildPhysicalChipList()`側でport単位に突き合わせて`PhysicalChipInfo::subDevices`を復元するよう修正した(stereoPairPortで束ねられるデバイスも2026年8月に対応済み。下記のL/R分割表示の項参照)。この開発環境にはHWプラグインDLL・物理チップが無くビルド確認までしかできていないため、この修正自体のユーザーによる目視再確認が未実施。またサブデバイスの表示順(現状Config側の生成順)をユーザー例示の並び(例: OPNBならFM→PA→PB→SSGの順)に揃えるための並べ替えは未対応のまま(表示は生成順)。**恒常的無効チャンネルの非活性表示(2026年7月追加)**: OPNBのch0/ch3(実機に存在しない、`COPNB`が`enableCh(false)`で無効化)やOPL/OPLL系リズムモード時のch6-8(リズム専用に転用、`chState_[i].disable()`)は、既存の`ChState::isEnabled()`(`status != Status::Disabled`)をそのまま`PhysicalChipChannelState::enabled`/`FITOMLevelChannel::enabled`として伝播し、`LevelMeterPanel`側でバーを薄暗いブランクプレースホルダ(枠線・塗りつぶし無し、ラベルも減光)として表示するよう対応。判定はチップ種別ごとの特別扱いをせず、既存の`ChState`の無効化状態をそのまま使うだけで済んだ

### FitomIFTest 側の追加作業

`plugin_sdk/include/fitom/IHWPlugin.h` に従った共有ライブラリ (`fitom_hw.dll`) のビルドターゲットを FitomIFTest に追加する。
`docs/DESIGN.md` の「FitomIFTest 側の追加作業」セクションに実装パターン付きで記載済み。

---

## ビルド手順

**依存関係の取得（既定: vcpkg不要）**

```bash
# nlohmann-json は git submodule (初回のみ)
git submodule update --init --recursive

# boost (thread/log/log_setup/format/interprocess) はシステムパッケージマネージャで取得
# Ubuntu/Debian の例:
apt install libboost-dev libboost-log-dev libboost-thread-dev
# Windows は公式バイナリ配布や MSYS2 等で入手するか、
# 後述の vcpkg プリセットを使う
```

**CMake 設定・ビルド**

```bash
cmake --preset linux-ninja .          # Linux (vcpkg不要)
cmake --preset windows-vs2022-x64 .   # Windows (vcpkg不要、boostは別途用意)

cmake --build build/linux-ninja
ctest --preset linux-test
```

**vcpkg を使いたい場合（任意）**

boost をシステムに用意しづらい場合など、vcpkg 経由でも取得できる。

```bash
cmake --preset windows-vs2022-x64-vcpkg .
```

このプリセットは `FITOM_USE_VCPKG_JSON=ON` を指定し、`vcpkg.json`（`boost-thread`/`boost-format`/`boost-log`/`boost-interprocess`のみ、`nlohmann-json`/`boost-asio`/`libftdi1`は含まない）経由でboostとnlohmann-jsonの両方を取得する。
